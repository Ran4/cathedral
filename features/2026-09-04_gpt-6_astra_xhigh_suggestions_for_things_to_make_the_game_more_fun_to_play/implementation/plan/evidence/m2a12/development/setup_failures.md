Initial apply_patch attempted an absent floor module-header context and made no changes. Subsequent insertion used the observed first use declaration. No Cargo had run.

The first owner_initial run (3 passed, 2 failed, 2 ignored) showed fixture setup drift: independently seeded Engines with the minimal test nav emit missing lamp keepers in HashMap iteration order. The supported new rare fixture now authors its diagnostic text explicitly; paired ordinary continuation restores exact covered initial authority before comparing polls. Production diagnostic order is untouched. The original 98,535-byte combined log remains /tmp/alibi-m2a12-owner_initial.log and is deterministically archived.

The owner_fixed focused-library run (165 passed, 1 failed, 25 ignored) showed a saved-backbone test harness attempting a second retained LoadCandidate reservation from one cohort. The harness now gives the already-admitted backbone and tested continuity component separate budgets and drops each continuity candidate before another decode. This proves saved-reference binding only, not the unresolved complete-envelope lifetime admission. The original 23,608-byte combined log remains /tmp/alibi-m2a12-owner_fixed.log and is deterministically archived. The subsequent final focused command covers the correction.

The original authored_smoke.json is preserved unchanged. After the final saved-reference harness fix, authored_final_smoke.json and populated_final_smoke.json were collected under the complete final 917-path freeze. Their primary witness streams include every Speech publication emitted by intermediate bounded polls; large WorldMotion debug text is counted/fingerprinted in a streaming writer rather than stored as multi-megabyte strings. No measured Engine authority is privately edited or sorted.
