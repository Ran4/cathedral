Status: M2a12 component implemented, verified and independently accepted (2026-09-09); see [coordinator review](coordinator/review.md). Complete M2 and host adoption remain pending.

# M2a12 floor/Engine continuity handoff

This bounded component preserves the exact existing ConversationFloor and remaining Engine cadence/publication/configuration authority. The public surfaces are `floor::checkpoint::{FloorDtoV1,FloorCandidate}` and `engine::continuity_checkpoint::{EngineContinuityDtoV1,EngineContinuityCandidate,EngineContinuityCheckpointContext}`. Each decode is aggregate-admitted before typed allocations; leases remain attached to opaque read-only candidates. No constructor, poll, service call, production installation or ordinary behavior change is added.

Final verification passed on the frozen 917-path component-inputs-v2 source map (`8da3e505bf2a0fa97e3cf569aa63bb6d3be3074f1d34b48b2055400ed2ed53e0`): 166 focused checks, all 6 independent public tests and 2,090 workspace tests across 44 targets (0 failed, 33 ignored). The evidence verifier checked all 17 recorded commands, every original/source-map hash, and deterministic gzip archives. Cargo was explicitly ceded after workspace exit; no source changed afterward. The original workspace log is `/tmp/alibi-m2a12-workspace_serial.log` (264,053 bytes, SHA-256 `d37d71d7adb5efbfd7cfbc8ab3a7223a25e7e4d10e474d112999db838139d23d`).

[Owner coverage](OWNER_COVERAGE.md) names every covered field and actual consumer rule. [Admission](ADMISSION.md) proves the concrete bounded layouts, sparse-copy behavior, zero additional validation working bytes and unchanged aggregate ceilings. [Source freeze](source_freeze.json), [complete source map](source_hashes.json), [commands](commands.json) and [verification](verification.json) bind final mechanical checks to exact inputs and original combined logs. The component-inputs-v2 helper remains unchanged. The [installed standard-library audit](development/stdlib_allocation_audit.json) pins String/Vec cloning and growth.

New supported component fixtures, all separate from historical bytes:

- `crates/cathedral-sim/tests/fixtures/checkpoint_continuity/floor-initial-v1.json`: virgin floor with signed-zero boundary.
- `crates/cathedral-sim/tests/fixtures/checkpoint_continuity/floor-active-v1.json`: refreshed insertion order, arbitrary exact event strings, scoped speech, reading pacing and microphone hold.
- `crates/cathedral-sim/tests/fixtures/checkpoint_continuity/engine-initial-v1.json`: never-sounded/never-ready state, explicit ordered diagnostics, raw NaN view-cone bits, signed-zero stored cooldown, infinite stored grace and nullable-message policy.

Private continuation first scrambles covered fields, restores the candidate without a constructor, asserts immediate canonical equality and then compares ordinary behavior. Floor witnesses cover cap trim-before-refresh, last-same-scope acknowledgement's 0.4-second beat, duplicate/unknown acknowledgement no-op, exact-deadline purge without beat, protected/background/read-only pacing and rolling/cleared player hold. Engine witnesses cover initial/cached publication, readiness, diagnostics, sound cooldown, floor/cadence continuation, raw constructor outputs and saved-backbone binding. The saved-backbone reference test deliberately uses separate component budgets; it does not claim full-envelope coexistence. Rare/maximal/sparse states are private tests, not mutations of measured Engines. Root independently owns `tests/checkpoint_continuity_boundary.rs`.

The six-phase `cathedral-backends/examples/alibi_continuity_cost.rs` probe builds real authored 520 and successfully placed +2,000/2,520 populations with real navigation/content. It uses ordinary commands and recorded provider/TTS values to retain one foreground awaited line, a separate reading hold, microphone hold, current/configured TTS difference, sound cooldown anchor and cached publication/cadence. All 25 setup polls are <=50 ms and zero discarded-time diagnostics are mandatory. Witnesses preserve full original prompts and actual budgets, all five Speech publications including intermediate polls, four TTS inputs/acceptance outcomes, two acknowledgement IDs and compact meaningful output; large motion output is counted and fingerprinted without retaining massive Debug strings. Debug raw timing samples establish functioning phases, not release latency or host-frame acceptance.

Development failures are retained in [setup failures](development/setup_failures.md), their original `/tmp/alibi-m2a12-*.log` files and deterministic gzip archives recorded by commands.json. The first failure was nondeterministic missing-lamp diagnostics in minimal-nav test seeding; the second was a duplicate retained cohort in the new reference-test harness. Both corrections change only the private setup. The original smoke, final-source smoke and later formatting-only freeze evidence remain distinct originals.

Remaining limits: full M2a envelope and owner/root/config/manifest agreement; all-consumer numeric/calendar horizons; actual Running/Save/Load/retiring lifetimes; M2b capture/hydration; SpeechRouter interruption with unsent drafts and owed readable committed speech; new-generation external retries; and M3 independent initial host publication. `Engine.transcript` remains the existing omniscient session artifact, with player-history policy unresolved. Exact Floor capture does not resume old audio/microphones, and no committed say is replayed. The earlier naive backbone+Round Save+Load peak already exceeds 1 GiB before Running; no ceiling was raised and this owner does not establish a complete save or synchronous host-frame budget.

The long serial workspace build was inspected through two finite read-only samples from the existing tmux server, without opening a pane/window, restarting Cargo or recording unrelated process arguments/environment. [Resource provenance](development/workspace_progress_provenance.json) preserves both original samples and the helper hash: rustc advanced 54.65 CPU seconds and wrote another 398,647,296 bytes over 56.099 seconds. This was a compiler progress diagnostic, not a component allocation or runtime performance measurement.

Coordinator acceptance verifies 2,090 workspace passes, 166 focused passes, six public boundaries and all 3,600 release phase samples. authored export pooled p99 0.002510 ms, observed maximum 0.003357 ms; populated export pooled p99 0.003217 ms, observed maximum 0.003654 ms. The [performance record](performance/README.md) retains all samples and the complete-save/host limitations.
