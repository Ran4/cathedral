# M2a16 owner handoff

Implementation is frozen and the full workspace verification passed. Root owns
release build/probes, complete release-image fixture publication, independent
acceptance and commit. The owner has ceded Cargo; every owner process exited.
No commit, push or subagent was performed. The unrelated user feature and
`docs/codex_gdd/`, `gauntlet/`, `reference/` remain untouched.

## Implemented authority

`cathedral_sim::checkpoint::complete` exposes a closed V1 envelope, mandatory
role-bound manifest, stable saved WorldIdentity, authored/populated profiles,
consuming admitted input and immutable read-only candidate. All sixteen complete
categories are validated from the exact saved bytes against actual compatible
installed definitions. Their complete typed expansion accumulates before any
owner drops, within the unchanged128 MiB ceiling. Raw input/offsets/proof/manifest
stay private and immutable; the candidate cannot detach its raw ownership from
admission. It can be sent and dropped without leaking the Load cohort.

The host wrappers capture the actual existing `HostCaptureSet` boundary. They
preserve accepted time/debt and H/physical state, current unread-message cursors,
readable originals and pending commands. They neither poll/drain to manufacture
eligibility nor call Engine::new/Round::seed. Nonempty World.events explicitly
refuses. Borrowed/count-bounded save output retains final source/boundary checks,
including generic HostCheckpointSource interior-mutability defenses and exact
legacy host record ordering.

Composition checks root union, all local owners, saved config/player/time,
publication/revision/lamp state, current floor/social/scheduler/Night/cognition
relationships, and concrete next-consumer numeric/clock/retained Round horizons.
It preserves explicitly checked-refusal and wrapping MAX counters. Stable saved
lineage is distinct from replacement RuntimeGeneration and the currently running
host's newly allocated WorldIdentity.

Compatibility binds actual ordered seed, independent installed role copies,
parsed prompt environment, geometry/area adjacency/catalogs/configuration,
compiled production inputs/toolchain/target/build settings, and the exact host
image hashed once from `/proc/self/exe` at host construction. IO and randomness
remain outside pure simulation. Missing identity makes full capture unavailable
without changing ordinary startup. Git HEAD is supplemental evidence only.
Exact-image identity deliberately allows debug/release/separately linked images
to reject each other's complete saves.

The one shared four-cohort1 GiB budget retains actual raw Vec capacity, bounded
save staging and JSON/diagnostic scratch, complete typed expansion, reconstructed
indexes and immutable candidate storage. Internal subleases reserve before
allocation and keep their cohort alive to the last owner. Owned definition
resolver storage drops before scratch shrinks; failure owners release raw storage
before their lease. The512 MiB named Running reservation is a trusted caller
minimum, with the narrowly supported500 MiB construction/capacity decomposition
in [owner-design.md](owner-design.md); it is not a theorem about all live Engines,
ECS, renderer assets or backend service heaps.

## Verification and exact frozen identity

The final command is `cargo test --offline -j1 --workspace`, explicit headless/fake
and six build/wrapper variables removed: **2178 passed,0 failed,40 ignored across
46 groups**, including all12 independent public complete tests. Earlier final
all-target check passed; targeted private allocation/enum/World.events tests
passed9/0. Root independently verified formatting and installed-library excerpts.
Every command attempt, including compile failures and malformed-map regression,
is preserved with the original effective environment and source evidence.

[Final command artifact index](owner-commands/final-artifacts.json) and
[command history](owner-commands/README.md) retain the original start/result and
lossless mtime0-gzip logs. Final workspace log SHA-256:
`ce014fc41ca142925a6acf149ec637731d83aad1db4d877b22b49230cd5b906a`.
Final source map is961 files (`component-inputs-v2`), SHA-256:
`fe370c6f8a82815eb0d806f2975cee1a5db6814cfcbfafb0d56657c1c72ce53b`.
It remained unchanged throughout final14. Root release starts from that same map.

The final debug test image is copied to
`/tmp/alibi-m2a16-debug-reference-binary`, SHA-256
`350a576ec6cf59ffc1596ce60f7a5a5f062f3a38f7e10e19d791eb6e4a61be07`,
2,283,869,832 bytes. Its exact final14 identity was checked after the successful
command; [original copied-image metadata](debug-reference-binary.json) records
the earlier source-at-image map. The only later frozen input delta was two
runtime-read legacy component fixture outputs.

Final10 correctly refused old M2a15 host fixtures after the existing source-bound
installed_catalogs role observed the appended cfg(test) city capacity method.
[The explicit refresh](host-component-fixture-refresh/record.json) preserves
exact predecessor bytes, the three fresh existing writer commands and originals,
and the complete diff: only `/scalars/definitions/installed_catalogs` changed.
No Rust source changed during this repair and no identity was normalized. Root
will update the still-historical fixture README at final publication, with that
README-only delta distinguished from complete fixture outputs.

## Remaining stages and reuse seams

Root's independent release observer collects separate definition/preflight,
borrowed encode, raw parse, typed decode/index validation, typed disposal,
candidate retention, input copy and final drop costs, including cold samples and
all tails. Reports carry schema1 and scenario `actual-complete-host-boundary-v1`,
actual520/2520 placement/workload counts, per-category expansion and capacity
inventories. The earlier debug populated expansion was133,351,183 bytes, only
866,545 bytes below128 MiB; simultaneous component maxima are not promised to fit.
Final release metrics and narrow headroom must remain explicit.

Complete fixtures must be generated from the final copied release test image
using `m2a16_complete_probe` with an explicit initial `ALIBI_COMPLETE_WORLD_ID`
before Engine construction, then validated by that same image. The retained
debug image can run `m2a16_verify_complete_fixture` with
`ALIBI_COMPLETE_EXPECT_IMAGE_MISMATCH=1` for honest other-image refusal. All
complete fixture IO is runtime-read; never patch a saved manifest to match a new
binary. Root owns the final fixture-only source-map delta and current fixture
README publication.

M2b still owes dedicated full typed hydration into production owners from the
validated candidate. It must retain or exactly rebind immutable definitions to
the candidate manifest before hydration, preserve all saved owner authority and
stable lineage, and budget the typed hydration/Running/retiring lifetimes. It
must not seed an Engine/Round to fill missing state or treat a future resolver's
changed identity as authorized by this candidate.

M2c still owns external work retirement/retry/adoption: exact original method,
prompt and token budget, held terminal success/error, scheduler fairness and old
inbox versus later arrivals, Night owed day/incarnation, and joint speech/floor
interruption semantics. No invented composing==floor-hold invariant is enforced.
M2d owes full continuation proof; M3 owns file/UI publication, actual ECS/assets/
backend service and retiring admission, generation coordination and host-frame
acceptance. Complete capture here is synchronous and makes no frame-budget claim.
