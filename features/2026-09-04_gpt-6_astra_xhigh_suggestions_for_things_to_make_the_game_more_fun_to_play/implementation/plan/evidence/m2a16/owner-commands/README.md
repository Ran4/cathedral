# Complete-envelope owner command record

Cargo attempts used the existing offline cache, `-j1`, explicit rustc/rustdoc
paths and no visible Bevy window. Fixture writers execute the preserved test
binary directly with the same headless/fake environment. Attempts are retained in order; failures are
not discarded or relabeled as final verification. The compressed logs decompress
exactly to the original `/tmp/alibi-m2a16-*.log` bytes; SHA checks and artifact
identities are in `development-artifacts.json`.

| Attempt | Result | Meaning |
| --- | --- | --- |
| check-01 |compile refused|Private LawCheckpointContext import corrected|
| tests-02 |compile refused|Borrowed unread-message lifetime corrected|
| tests-03 |11 passed,1 ignored|Initial10 independent cases and real authored smoke; before final hardening|
| tests-04 |1 passed,12 failed,1 ignored|Defensive scalar forwarding broke numeric map keys; corrected by explicit wrapped map-key mode|
| check-05 |passed|Corrected map-key decoder compiled|
| tests-06 |compile refused|Standalone headless explicit EngineConfig needed optional identity fields; no new host tests ran|
| tests-07 |33 passed,0 failed,2 ignored across44 groups|All12 independent cases, actual authored/+2000 and malformed typed errors; before final resolver/capacity diagnostics|
| check-08 |passed all workspace targets|Final owned resolver and inventory/test seams compile|
| tests-09 |9 passed,0 failed|Private complete allocation, custom PlaceRegistry indexes, enum payload errors and event fence at final source|
| workspace-final-10 |2157 passed,1 failed,40 ignored across43 groups|Existing host fixture installed-catalog hash changed because its source role includes the new test-only city capacity observer; exact refusal and prior fixtures preserved|
| host-fixtures-11/12/13 |1 passed each|Three fresh existing writers produced exact identical current initial/active component fixtures; only installed_catalogs JSON value changed|
| workspace-final-14 |2178 passed,0 failed,40 ignored across46 groups|Full workspace after reviewed two-fixture refresh, unchanged961-file final source|

Attempts01–03 used the early broad tracked-file source record, which included
unrelated evidence/binary paths. Attempts04–05 used the focused shared source
enumerator and explicit headless/fake flags. Those historical attempts did not
record helper identity and a final source comparison at start/end; the retained
wrapper snapshots cannot manufacture missing start evidence. The original
effective environment remains in each start record rather than being rewritten
to resemble later commands.

Version3 (attempt06 onward) records its own SHA, the shared source enumerator
SHA, focused `component-inputs-v2` sources before/after, effective headless/fake
environment and absent build flags; it refuses overwriting an attempt. Version4
additionally removes the six build flag/wrapper keys explicitly and returns
nonzero on a source delta. All final commands use version4. Source edits between
development attempts are intentional; final acceptance refers to the separately
frozen961-file map and final command artifacts, not an earlier successful run.

Final acceptance command is `workspace-final-14`; its start/result and exact
lossless compressed log are retained here. [Final artifact index](final-artifacts.json)
records hashes, actual group/test totals and the preserved debug image. The
source-at-command map is also inside each start record. The successful freeze is
`fe370c6f8a82815eb0d806f2975cee1a5db6814cfcbfafb0d56657c1c72ce53b`; its only
change from the separately preserved pre-repair map is the two reviewed legacy
component fixture outputs. Production and test Rust sources stayed unchanged.

The debug reference executable remains at
`/tmp/alibi-m2a16-debug-reference-binary`, SHA-256
`350a576ec6cf59ffc1596ce60f7a5a5f062f3a38f7e10e19d791eb6e4a61be07`,
2,283,869,832 bytes. It was copied before fixture refresh and verified against
the final14 executable; no fixture output is compiled into that image. Its
metadata records the original source map and the final artifact index records
the successful current-fixture source map. Root owns subsequent release-image
measurements, complete fixture publication and the explicit debug-image refusal
check; this owner record does not claim those later outcomes.
