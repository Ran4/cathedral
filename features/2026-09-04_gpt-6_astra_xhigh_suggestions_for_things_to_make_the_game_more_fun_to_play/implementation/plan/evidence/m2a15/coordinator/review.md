Status: Accepted host component cut (2026-09-14). Complete M2 and host frame integration remain pending.

# M2a15 independent review

This leg adds admitted read-only host continuation records. Its predecessor is
M2a14 commit `e4653edfdbfcc0eec75a2bb6bd351e9b0eb224ae`. The implementation
owner is the sequential fresh-context `m2a15_host` agent; the coordinator owns
the independent public test seam, release evidence and final commit.

The actual capture set follows the completed ordinary bridge drain and precedes
mirror reconciliation and subsequent input forwarding. Observation does not
poll, consume input, install state, wait for a provider or require later worker
arrivals to stop. Definition checks cover the installed collision/virtual-floor,
gate and vermin owners, including required presence and static configuration.
Saved-component contexts borrow admitted backbone/law/knowledge/marks/climate/
animals/ledger candidates and revalidate their shared boundary before use.

The ordinary chat regression found during review is repaired at forwarding:
unaccepted pose-bearing input receives a current spatial identity while retaining
its captured words, position and target. The simulation's stale-position check
and accepted command identities remain intact. The independent test confirms a
committed line and a separately waiting typed line coexist in the checkpoint,
then the next ordinary update commits the waiting line.

Original readable speech is retained under a shared 8 MiB byte budget, reserved
before cloning. A trailing lease releases capacity after the receipt data drops;
subtitle, bubble and player-caption lifetimes retain their shared charge.
Exhausting this auxiliary allowance preserves ordinary display/audio and refuses
capture while required readable authority is unavailable. An explicit player
caption flag distinguishes unavailable committed originals from provisional text.
Replacement, ordinary expiry and generation reset clear the flag with its owner.
The actual host test checks refusal and recovery, including NPC originals; direct
consumer tests verify no early acknowledgement or lost dialogue. The earlier
ack/skip implementation and its failed checks remain historical evidence only.
Admission checks include raw input and the concrete row stride before typed
allocation, preserve charges through candidate validation, and reject a mutable
borrowed source that exceeds its measured bytes or row allocation.

## Development evidence reviewed

All eleven coordinator public tests passed in `workspace-final-2`. They cover repeated
read-only capture, mandatory/closed observed fields, unit aliases and duplicate
keys, raw padding/admission, post-boundary worker arrivals, mutable-source attacks,
definition/physical/input binding, saved-component context agreement, journal
scroll position, exact Unicode drafts, and waiting input alongside committed
speech. The newest speech-consumer counter corruption is added to the existing
binding case and passed in the unchanged-source first workspace run.

`host-tests-5` passed 24 tests and failed one owner fixture assertion about box-collider
count; actual full-city capture succeeded. The corrected fixture accounts for
both boxes and convex prisms. Earlier failures and their original
logs remain preserved. [The initial archive audit](development-archive-audit-1.json)
checks all six then-completed development commands; it is not final acceptance.

The first workspace run passed 2,132 tests, failed two owner fixtures and ignored
37 tests before Cargo stopped. All eleven independent tests passed. The failures
concern an explicit command-gap fixture that exceeded the host's enqueue window
and a stale-generation test that treated legitimate reader-cursor progress as a
presentation mutation. Both fixtures were corrected against the actual
consumers; their admission and stale-content rules stay in force.
[The second archive audit](development-archive-audit-2.json) verifies all nine
completed commands and preserves those failures. Two development runs recorded
source changes during execution and are expressly not final-source evidence.

`workspace-final-2` then passed 2,156 tests with no failures and 37 ignored in 46
groups. Its 947-input source map and owner check are preserved under
`before-fixtures-*`. The first immutable release build also completed against
that earlier map; it is not evidence for the final fixture-inclusive source.

The missing persisted component fixtures were closed with a test-only addition
and two new canonical payloads. Three fresh writer processes emitted identical
initial (3,549 bytes) and active (6,275 bytes) states. The ordinary loader checks
their canonical roundtrip, admitted candidate and actual compatible CityPlugin
boundary. Only a detached copy used for comparing the fresh export aligns the
replacement generation; payload, candidate and live identity remain unchanged.
Originals and mtime-zero gzip archives are retained. This is component load
evidence, not whole-city hydration or admission of the fixture World.

The release auditor retains all six phase arrays, cold samples and every sample
above 2 ms or 30 ms. It verifies exact original/gzip data, command-at-start source
and binary identities, actual citizen placement, readable workload and unchanged
simulation counters. [Its latest negative checks](auditor-negative-checks-3.json)
reject thirteen deliberate corruptions. These synthetic checker inputs are not
host execution evidence.

## Final verification and acceptance

The final 950-file source map is
`5251ea6af940cce508c66111573bcdf1d53343207e7dea1ee2e5dbc048f62b6a`.
`workspace-final-3` passed **2,157 tests, zero failures, 38 ignored in 46 groups**.
The [independent source audit](source-audit-final.json) verifies that exact
unchanged command-start map, all eleven public cases, the closed wire/layout
test, scoped formatting of 36 Rust files and the clean whitespace check.
[Archive verification](owner-archive-audit-final.json) checks all nineteen owner
commands, including failures, and both development smoke originals.
[Fixture verification](fixture-audit.json) independently checks all three writer
outputs, the persisted payloads, their exact archives and successful loader
provenance. No workload rerun was substituted for examining those originals.

The final [release build audit](release-build-audit-final.json) passed for immutable
binary `e6be43620573a23046479444fc80ca7c3f735e90fd991d491d24ee049506fd3a`.
Both [actual-city release smokes](smoke-audit.json) passed. Six independent timing
processes retain 3,600 phase samples; the [performance audit](performance-audit.json)
verifies all primary originals, source/binary/environment/helper identities,
actual placement, exact repeated costs and unchanged revision/event/input counters.
The [measurement record](../performance/README.md) preserves the full distributions
and distinguishes process RSS from component admission.

Both workloads encode 6,275 bytes and retain a 10,162,498-byte simultaneous
component Save plus Load charge, excluding Running. Export p99 is 5.634495 ms
authored and 10.245549 ms populated. All preflight/export samples exceed 2 ms;
four populated decode/candidate samples also do. All 1,204 slow samples and the
cold samples remain recorded; none exceed 30 ms. These costs rule out placing
the current extraction synchronously in the planned host-frame budget. Bounded
offload or incremental coordination remains M3 work under unchanged limits.

Accept M2a15's strict read-only host component, ordinary chat/receipt fixes,
supported fixtures, source/admission verification and release evidence. No source
changed during the final workspace run, release build or measurement processes.
The next sequential cut is complete-envelope composition, M2a16, after this commit.

This component does not establish complete World/Engine hydration, full-envelope
cohort admission, once-only external-work adoption, disk publication or a host
frame budget. The known renderer/full-stress limitations remain recorded at M0;
no GPU reprobe or visible application window is part of this leg.
