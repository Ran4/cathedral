# M2a15 owner handoff

Coordinator addendum, 2026-09-14: [component acceptance](coordinator/review.md)
is complete, including both final release smokes, 3,600 audited timing samples
and source/fixture/archive checks. [Measured costs](performance/README.md)
retain the pending full-envelope and host-frame limits described below.

Owner implementation and verification are complete. The coordinator received
sole Cargo ownership at the successful final workspace exit and owns independent
release measurements, final acceptance and commit. No owner commit or push was
made. Runtime source is frozen; subsequent owner work is documentation only.

## Final supported implementation

The new pure `checkpoint::host` module provides a closed, admitted V1 scalar and
typed-row component, borrowing source strings before measurement. Context binds
actual Engine authority or coherent admitted saved candidates without creating
an Engine/World. Decode has no public unchecked Deserialize route. Reservation,
row-stride and bounded expected-publication comparison protect every codec pass.

The executable `HostObservation` reads the actual completed ordinary boundary
after DrainBridge and before later gameplay producers. It records exact body,
time/debt/residual, command allocator and consumer fences, actual installed
physical/gate/vermin definitions, soundscape semantic gates, historical UI intent
and exact surviving readable obligations. Pending worker input does not require
an empty command queue. Read-only capture neither polls nor consumes Messages.

Runtime changes make formerly implicit reader/message sequences explicit and
retain bounded original speech receipts across independent subtitle, bubble and
caption lifetimes. Provisional replacement/expiry/disconnect/generation reset
clear old caption authority. Receipt retention saturation preserves normal
display/audio and acknowledgement timing; capture explicitly refuses missing
originals until their readable lifetime ends or is replaced. Existing queue
overflow behavior and all earlier global caps remain unchanged.

A confirmed ordinary chat regression is fixed at the forwarding boundary:
unsubmitted pose-bearing UI commands receive a fresh monotonic spatial identity
while retaining their original frozen pose and other inputs. Already accepted
commands and sim stale-sequence rejection are unchanged. Pure gate blocking is
available without advancing animation or emitting a cue.

## Source families

- New codec and validation: `crates/cathedral-sim/src/checkpoint/host.rs` plus
  `host/{wire,context,ordering,publication}.rs` and the checkpoint module export.
- Borrowed saved/actual context: narrow getters in Engine, backbone, ledger and
  law/knowledge/marks/climate/animals candidate owners. Historical wire is unchanged.
- New actual adapter: `src/host_checkpoint.rs`, `host_checkpoint/records.rs`,
  owner `tests.rs` and coordinator-owned `tests_public.rs`; module registration
  and `HostCaptureSet` scheduling.
- Actual owner access/continuation: controller/live time; city gate/vermin/marks/
  CutMargin; bridge/local engine/runtime; interaction/chat/inventory/custody;
  speech/HUD/journal; soundscape. Access changes are narrow and read-only apart
  from the documented ordinary receipt, cursor and unsubmitted-spatial fixes.
- Evidence helper: `evidence/run_m2_host_checks.py`. Source scope remains v2,
  including `default_config.ron`; no additional runtime file input was introduced.

## Completed checks and archived failures

`workspace-final-3` passed **2,157 tests, zero failures, 38 ignored in 46 groups**
on 2026-09-14, including host 569/0/5 and sim 799/0/28. All eleven coordinator
public host tests passed. `source_hashes.json` exactly copies the command-start
950-file map and matches current source; its result records source unchanged
throughout. Its file SHA-256 is
`5251ea6af940cce508c66111573bcdf1d53343207e7dea1ee2e5dbc048f62b6a`.
The prior successful `workspace-final-2` (2,156/0/37) and its 947-file map are
preserved as pre-fixture evidence; the coordinator also retained release build 1.

The supported-fixture requirement is now closed with exact initial/active V1
payloads under `crates/cathedral-sim/tests/fixtures/checkpoint_host`. Three fresh
writer processes agree byte for byte (3,549 and 6,275 bytes). The ordinary loader
test and full workspace decode at actual compatible CityPlugin boundaries,
check exact canonical decoded/candidate bytes, and compare the fresh export
using only a detached generation-fence adjustment. Stored and live identities
remain unchanged. The final fixture delta touches only owner tests and the new
fixture pair/README; no production/public seam or historical payload changed.
`owner_fixtures/fixture-equality-1.json` binds originals, hashes and mtime-zero
archives. All 19 owner command records are retained, including writer 2's PATH
typo and the required-environment repeat, and the intentional source-changing
fixture copy command.

All failed development commands remain archived with exact original stdout and
stderr, start identity and deterministic gzip. They cover initial compiler
integration fixes, the independent test exposing the real stale chat sequence,
owner initial Load reservation and box-count assumptions, the host enqueue-gap
fixture assumption, the existing Debug comparison's newly explicit stale-reader
progress, and the expected diagnostic prefix in the new receipt-refusal fixture.
The final corrections have successful targeted and whole-workspace evidence.
Commands that overlapped later source edits are explicitly marked development
only; they are not attributed to the final source snapshot.

Both meaningful renderer-free city smoke probes passed. Authored: 520 characters,
18,496 entities. Populated: all 2,000 additions placed, 2,520 characters and
73,984 entities. Both use 751 actual boxes, 1,153 prisms, two gate barriers,
CutMargin, eight vermin colonies/150 rats and the same 22-row readable workload.
Repeated bytes and revision/event/H are identical within each process. Full raw
JSON and command logs remain in `/tmp` with hashed archives under this directory.
These two-sample test-profile runs establish fixture integrity, not release tails.

## Explicit remaining gates

The coordinator's release runner owns repeated timings and process RSS. This
component does not establish a synchronous host-frame pass. The development
timings are test-profile costs; final release measurements and whole-host
scheduling must be assessed separately. Query/entity scans, actual definition
hashing, private fixture allocation and service/render ownership have not been
hidden from the declared measurement scope.

Complete envelope/root/category/config/manifest agreement, M2b hydration, M2c
once-only retry/SpeechRouter interruption adoption and M3 new-generation initial
publication remain pending. No production partial restore was added; behavioral
restore evidence uses only deliberately scrambled private prepared owners.

Actual Running/Save/Load/retiring lifetimes are a later gate. The earlier naive
backbone plus Round Save+Load sum is already 1,256,093,444 bytes before Running,
above the unchanged 1 GiB cap. Host component success does not solve that excess
or establish full save/load or save-anywhere acceptance. No GPU, audio, provider,
visible window or live-human gameplay acceptance is claimed by these tests.
