Status: implementation and frozen-source verification complete; Cargo/source ceded to coordinator for acceptance (2026-09-15).

# M2d owner handoff

M2d adds complete deterministic future continuation witnesses on shared
`develop`, following accepted M2c `d921864d32f44abc1c4428fe1b7052eb153d9670`.
The owner made no commit or push. The coordinator owns acceptance and the commit.
All owner Cargo/probe processes have exited. Explicit Cargo/source cession follows
the successful workspace and evidence seal; no further owner source edit or
verification command is planned.

## Implemented boundary

The cfg(test) harness uses public complete capture, installed-definition
validation, fresh admitted hydration, pending-work preparation and inert fresh
service binding. Its actual Admitted<PreparedContinuation> retains the Engine,
typed lease, asset lease and service lease through ordinary Engine polls,
comparison and disposal; its scoped Running allowance outlives those owners.
No public extraction/poll/adoption API, production behavior or persisted DTO was
added. The external CPU Host begins with actual decoded scalars and owned
records, then advances accepted time, physical samples, command/spatial
watermarks and actual sampled publications. The prepared saved Host remains
unchanged. These diagnostic copies are behavioral evidence, not an aggregate
heap or frame-time measurement.

Twelve owner tests cover active reserved food work plus generated resident motion,
a serving FIFO market queue, pending road departure, a warm conversation,
custody escort motion, weather transition and old-slope bells, knowledge
propagation, held recorded success/error, unfinished Scheduler retry,
queued/recorded Night and unfinished Night. An initial fixture also checks the
basic complete pipeline. Two independent coordinator tests cover pending-offer
Identified acceptance/replay across repeated restores and nonzero accepted wall
debt/fixed residual with successive physical/command samples and complete loads.
Every meaningful scenario asserts its active capture shape and a later actual
transition, receipt, effect or physical displacement.

Every future message vector is compared. Every captured owner is deterministically
re-encoded and compared as bytes, preserving signed-zero distinctions. The
ordinary state exclusion is only `/host/scalars/boundary/generation`. Unfinished
Scheduler observations use the common admitted prepared representation while
the obligation is live; original pre-prepare flight fields, prompt, root and
incarnation are pinned, and only its exact replacement request identity differs.
One exact actor-specific LLM Thinking status is declared. Direct full equality
resumes after the recorded completion settles, including newer unread input.

Unfinished Night pins exact owed day/subject/incarnation/root and accepted
method/prompt/budget. The active replacement's two request-ID fields may differ;
its `next_attempt_at` is independently asserted to follow actual retry time.
All other authority is compared, and request-ID exemptions vanish after
settlement. Queued/recorded Night and held results require direct complete
state/event equality. The [design](owner-design.md) names every exact JSON path
and fixture precondition. No owner category or durable identity is ignored.

All future time advances use admitted 50/100 ms steps and exact Duration seconds.
The independent debt fixture uses actual AcceptedTime::admit at setup and
100 ms future wall/accepted steps, which preserve its nonzero debt legitimately.
No coarse future jump discards physical movement work; no post-capture private
owner repair or live scheduler closure is used to force equality.

## Frozen verification

The frozen 987-input component-inputs-v2 map SHA-256 is
`28a535a0b232d887c0e2653a308ee0c92eb62c9b04240b7aaa587886c2815188`.
It adds four Rust test files to the exact 983-input accepted M2c post-publication
map. All nine changed/new Rust files are formatted. [owner/seal.py](owner/seal.py)
publishes [verification.json](verification.json) and
[source_hashes.json](source_hashes.json) only after the final commands succeed,
checking every original command/helper/map/log/archive identity.

| Final command | Result |
| --- | --- |
| `focused-final-01` | 16 passed, 0 failed: 12 owner + 2 independent coordinator + 2 historical filter matches |
| `format-final-01` | All 9 changed/new Rust files pass scoped rustfmt --check |
| `workspace-final-01` | 2,229 passed, 0 failed, 44 intentional ignores across 46 groups |

The workspace command is `cargo test --workspace --offline -j1 -- --test-threads=1`.
Its [start record](owner/workspace-final-01-start.json) records absolute argv,
hidden/fake flags, full source scope, removed inherited build flags and explicit
toolchain/Cargo environment. Its [result](owner/workspace-final-01-result.json)
and [exact gzip archive](owner/workspace-final-01.log.gz) retain the raw
`/tmp/alibi-m2d-workspace-final-01.log` identity. Formatting and focused commands
use the same map; source did not change after their freeze.

Final simulation library: 838 passed, 28 ignored. Final host binary: 602 passed,
11 ignored. The workspace raw SHA-256 is
`ed96c16d6d8171e63b54901ad4cc8e4d2056ba9c2113c9a0a825af8d667d5950`;
its exact mtime-zero gzip SHA-256 is
`2229608f4cea319b1e94e0fad19c092a79b0233b219e9c17e7bcc01768a9887b`.
The owner seal succeeds for all thirteen commands and the current source map.

All thirteen owner command records remain, including seven failed development
attempts, two mutating formatting runs and a passing development scenario run.
[README](README.md) explains every failed attempt. Source-changing attempts keep
their original `sources_unchanged=false`; none is mislabeled frozen acceptance.
The coordinator independently owns the final provenance audit and acceptance.

## Reviewed limits and remaining milestones

The escort fixture proves continued movement of both officer and prisoner while
retaining custody/holder/intent identity. Its later ordinary endpoint is officer
5.9181795344551205 m and prisoner 7.418179534455124 m from the station, InCharge,
officer intent None. Both timelines match. Existing place-intent arrival is
6 m, station commitment 4 m, and escort trailing 1.5 m; this ordinary endpoint
interaction is recorded for later enforcement work, not fixed or presented as
successful commitment in M2d.

M3 retains actual Bevy/controller/host-time adoption, storage, initial publication,
asynchronous work, offload, swap, supersession and retirement. M0's renderer and
full-stress gaps remain. This test-only milestone required no repeated release
cost/image probes or admission-cap change. It claims neither live-provider future
prose equality nor aggregate multi-host heap/frame performance. Historical
same-image fixture bytes/manifests and preserved reference binaries are untouched.

No visible app, provider/device/audio/GPU or 20,000-resident probe ran. The
unrelated `docs/codex_gdd/`, `gauntlet/`, `reference/` and ambient feature work
remain untouched and outside the handoff.
