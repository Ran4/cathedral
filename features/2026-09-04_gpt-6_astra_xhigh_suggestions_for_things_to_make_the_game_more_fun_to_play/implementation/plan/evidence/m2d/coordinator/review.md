Status: M2d implemented and accepted after independent review and frozen verification (2026-09-15).

# Deterministic future continuation review

Accepted predecessor M2c is commit `d921864d32f44abc1c4428fe1b7052eb153d9670`.
Its final post-publication 983-input map is
`d5c73f9a292414d1e1c2a874ae18974c3d734f240507d61b3ca1f64917a24d2a`.
The source tested before publication had 980 inputs; only two verified runtime
continuation files and README were added afterward. Historical evidence and
creating-image fixtures remain exact.

M2c passes 2,215 workspace tests, all 39-file formatting, 600 release preparations
and eight fresh-process fixture checks. Its 13 independent continuation cases
and full mixed-obligation envelope regression are accepted. They do not replace
M2d's ordinary future-control comparison across every named gameplay boundary.

The agreed cfg(test)-only harness will retain Admitted<PreparedContinuation>
and all asset/service leases while ordinary Engine polls run. It will use full
capture/validation/hydration/preparation with fresh service handles and compare
every complete owner plus an explicit synthetic Host. Production quarantine,
no public Engine extraction, no unsafe Send and all numeric caps remain.

Review requires an asserted active precondition at each capture and a real
post-load event/transition. Equality alone must not conceal an idle fixture.
Physical continuation uses bounded admitted time steps; initial clocks should
be placed near needed events rather than skipping movement with coarse polls.
Canonical exclusions require exact field paths and justification: external
execution generation/request identity may differ, but semantic operation,
actor incarnation, original input, allocation counters, owner categories and
committed events remain checked. Unfinished requests use conservation/once-only
application where unsaved live-provider futures cannot be promised identical.

The Host fixture must explicitly preserve/rebind saved authority and advance
accepted logical time and spatial state after polls. PreparedContinuation's
public capture remains pinned to its pre-adoption boundary. A cfg(test) wrapper
must describe its separate Host observations honestly and cannot claim M3 ECS,
controller, UI, origin binding, publication or actual runtime ownership.

The sequential owner is /root/m2d_continuation and owns Cargo until explicit
cession. Root owns independent engine/continuation_future_review_tests.rs,
coordinator evidence, top-level feature/plan status and commits. The owner
provides a stable harness seam before the final source freeze. Verification is
bounded to focused meaningful continuation cases and a final unchanged-source
workspace run; no automatic historical cost, renderer, live-provider or 20k
stress reprobes.

## Independent review during implementation

The two coordinator-owned complete future cases passed in development run
`future-03`: a pending offer is accepted with one identified command, restored
again and replayed without a second transfer; nonzero ordinary wall debt and
fixed residual survive successive complete loads and ordinary spatial samples.
That run had seven passing and eight failing tests overall and its source map
changed during compilation. It is development evidence, not final acceptance.

Review corrected fixture assumptions without relaxing checkpoint validation.
The legacy control Engine uses generation zero while the explicit CPU Host has
a valid fence of one; restored engines use the saved Host fence plus one.
Generic fixtures need a small actual navigation graph so ordinary movement
ticks advance the saved movement clock. Installed market, production, road and
escort fixtures use the installed city graph. All later poll instants must be
the exact f64 seconds of their admitted Duration; multiplying a float step can
produce a different bit pattern than the saved nanosecond duration.

The future Host now owns the actual decoded scalar and record values. It
advances sampled journal/law/chalk projections using the actual Engine
publication caches and the shared resolved-journal formatter. This is explicit
CPU fixture behavior, not an assertion of Bevy projection or unread UI adoption.
All sampled records participate in complete capture validation and comparison.

An unfinished scheduler request has a different preparation representation from
an uninterrupted active execution. Comparing separately admitted observations
after the same production prepare transformation is permitted while this one
obligation remains unfinished. Require its original inputs, semantic identity,
incarnation and owned Knowledge roots explicitly; normalize only the stated
external request-ID path. The actual control timeline remains uninterrupted,
and direct full state equality must resume after the recorded result settles.

Unfinished Night resubmission intentionally sets its pacing deadline relative
to the new submission time. A separate conservation witness may assert the
precise original/retry deadlines and account for that exact field difference;
it must not call this general future-state equality. Queued Night reflection
and retained completions still require full state and event equality.

The coordinator audit helper checks every completed command's exact original
log against its mtime-zero archive, source-map/helper/enumerator hashes and
explicit build environment. Final acceptance will additionally require named
successful unchanged-source focused, formatting and workspace runs whose maps
match the final implementation, preserving every failed development attempt.

## Frozen final verification

Implementation review found no remaining requested source changes. The final
987-input map is
`28a535a0b232d887c0e2653a308ee0c92eb62c9b04240b7aaa587886c2815188`.
`format-final-01` passes scoped formatting for all nine changed/new Rust files.
`focused-final-01` passes sixteen tests: twelve owner continuation scenarios,
two coordinator scenarios and two pre-existing tests selected by `future_`.
Both commands report unchanged source and match the final map. The focused log
SHA-256 is `14c5b9492be6d68c15ce17d87a2dc4b35bc6d73707d831d64e5ec8a6bff4758c`;
its exact archive is `7796d2282b6cbcc778d1bb49dcbc00532d20a035409fe1efceb0e94e01f32c82`.

The strengthened unfinished Night case checks the original subject, owed day,
incarnation, semantic root, prompt and cognition input before preparation. While
the replacement execution is active, only two external ID paths are normalized:
`/night/night/night/in_flight/request_id` and
`/cognition_inputs/night/request_id`. Both original and replacement ID values
are asserted before normalization. The separately asserted retry-pacing delta
remains explicit. After settlement those ID exceptions are absent.

`workspace-final-01` passes 2,229 tests with zero failures and 44 intentional
ignores across 46 groups, including 838 simulation unit tests and 602 application
tests. Its source remained unchanged and matches the same final map. The exact
raw log SHA-256 is
`ed96c16d6d8171e63b54901ad4cc8e4d2056ba9c2113c9a0a825af8d667d5950`;
the gzip archive SHA-256 is
`2229608f4cea319b1e94e0fad19c092a79b0233b219e9c17e7bcc01768a9887b`.
The command took 1,335.514 seconds, including compilation; this is validation
elapsed time, not an application frame or checkpoint performance measurement.

The independent [command audit](command-audit.json) passes all thirteen command
records, including exact original/archive bytes, mtime-zero headers, helper and
enumerator identities, source maps and explicit build environment. All seven
failed development attempts remain recorded. The [source comparison](source-delta.json)
finds four new Rust test files, five existing files changed only for test
registration/helper access, and 978 predecessor inputs unchanged. No production
checkpoint schema or behavior changed in this leg.

M2d is accepted. M3a durable backend slots is the next sequential owner after
this commit. M3 still owns actual controller/ECS/time-origin adoption, storage,
publication, offload and retirement; the CPU Host here cannot certify those
behaviors or their budgets. The ordinary escort endpoint interaction is retained
as a later enforcement concern. M0 renderer/full-stress and later external
acceptance items remain pending under the developer's continuation authority.
