# M2b owner coverage

The production boundary remains the complete ordinary HostCaptureSet transaction.
Each roundtrip captures there, validates the exact raw envelope, drops Save,
hydrates from newly admitted installed assets and compares16 independently
computed source category hashes with serialization of the actual new domain
owners. World/Engine state is never externally mutable through HydratedEngine;
raw bytes do not survive hydration. Read-only source stamps and navigation
inventories remain equal before and after all work.

| Witness | Non-vacuous authority |
| --- | --- |
| Rich actual authored CityPlugin |520 characters, initial seeded domain owners plus ordinary readable/pending host workload; all16 actual-owner categories. |
| Rich actual populated CityPlugin |2,520 characters, all2,000 requested additional residents actually placed; same complete owner/category coverage. |
| Held scheduler plus interrupted speech |An ordinary fake scheduler completion is held while PlayerAudioBegin is active. Before capture scheduler in-flight and held fields are nonempty; Speech has one capture and one stream. Hydration retains both the exact scheduler input and the typed speech streams. |
| Submitted Night |Ordinary updates at the waning-day configuration reach an actual Night submission. Saved Night in-flight authority and exact prompt/duty survive hydration; all16 categories remain equal. |
| Accepted recording/root |An ordinary consequential PlayerRecording, staged with PhysicalPosition.current and PlayerSpatialState::position_for_action, is accepted by fake batch transcription before the next ordinary completion drain. The source has one accepted recording; the actual new World has one protected speech action and membership agrees with the retained semantic ID. |

The interrupted-speech fixture has no accepted recording and makes no claim of
nonempty recognized draft text or restored active audio. The submitted-Night
fixture has no held Night result. The separate accepted-recording witness checks
the newly reconstructed mutable root index independently of speech sidecar
serialization; substituting an empty index must fail. Existing M2a13/M2a14 component variant coverage
remains relevant predecessor evidence, but is not relabeled as new complete
hydration execution coverage. M2c still owes joint Floor/ledger interruption,
accepted-recording terminalization, held success/error consumption and exact
once-only retry. M2d still owes the complete continued-input/control suite.

Root-owned tests_hydration_public.rs independently checks all16 immediate hashes
after disposing the original App before fresh resolution; saved lineage and
logical boundary with a fresh runtime fence; retained asset Weak lifetime;
changed actual configuration, missing World navigation and changed host
definition refusal; zero/reused generation refusal; shared-pressure refusal
before factory invocation; preparation cancellation; factory error and all six
observer unwind points; and foreign-budget/same-budget-pressure refusal before
category staging. The only shared code is admitted installed-asset setup.

The owner structural test bounds moved root/control/index storage within256 KiB.
The shared-budget test proves equal numeric charges do not substitute for common
Usage identity and that a subordinate owner keeps the cohort occupied.

Ignored fixture writer/readers cover exact persisted bytes in independent
same-image processes, exact-image refusal, original parsed-asset resolution after
source App disposal and16-category new-owner equality. They are test IO seams,
not production M3 storage/adoption. Versioned fixtures must be generated only
after final source/image freeze; M2a16 fixtures remain historical unchanged.

All host tests/probes use hidden-window/fake-backend flags; no renderer workload,
20,000-resident case, real providers/audio/devices or live application adoption is
included. Diagnostic timings include the host-test allocation wrapper and are
not an ordinary production frame-budget claim.
