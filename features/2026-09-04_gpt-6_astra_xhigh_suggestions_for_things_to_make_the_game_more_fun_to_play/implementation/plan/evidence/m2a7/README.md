# M2a7 marks/catalog/switches and chalk publication — 2026-09-08

Status: Implemented and reviewed (2026-09-08). Final workspace, independent source/admission review and release audits passed. This is an existing component cut; complete M2 persistence and M3 adoption are not implemented.

WorldMarks/EngineMarks preserve the whole private chalk owner, exact immutable definitions, World switches, initial Engine config and exact nullable publication history. Orphan marks, historical references, raw scale bits, both Never clock sentinels, spent scrub days and sparse allocation history survive without reseeding or cleanup during load. Read-only candidates validate against a World or an unadopted backbone; production exposes no partial installation.

- [Coordinator review](coordinator/review.md) accepts this component and records the remaining full-envelope obligations. [Release measurements](performance/README.md) retain 3,600 audited phase samples; export p99 is 1.110/4.411 ms.
- [Owner coverage](OWNER_COVERAGE.md) lists every saved field, reachability exception and remaining composition obligation.
- [Admission](ADMISSION.md) proves the closed layouts, sparse containers, bounded borrowed definitions and original raw charge retention under unchanged limits.
- [Source freeze](source_freeze.json) and [831-file manifest](source_hashes.json) pin the final code, public test, fixtures and runtime JSON assets/lore before the serial workspace run.
- [Scoped source/format audit](development/source_scope_audit.json) proves ordinary source equals HEAD after only module wiring, verifies unchanged historical fixtures, and records the existing marks.rs test-comment alignment exception. Twelve scoped files pass rustfmt; the original and baseline exception logs remain.
- [Compiler and build environment](environment.json) records rustc 1.96.0 on x86_64 Linux and the explicit/inherited build settings used for final verification.
- [Exact command records](commands.json) retain every cargo invocation, explicit environment and raw log hashes. The [wrapper](development/run_checked.py) captures stdout+stderr before any summary. [Final verification](verification.json) checks all 12 command records, deterministic gzip archives and both compressed/decompressed hashes.

The frozen-source full workspace passes **1,986 tests, 0 failures and 23 intentional ignores across 39 targets**, including the six public marks boundaries. Focused checkpoint verification passes 92 tests, with 15 intentional ignores; the independent public boundary target passes six tests. The new layout diagnostic passes, reporting Marks 56 B, Mark 112 B, World DTO/candidate 224 B and Engine DTO/candidate 504 B. Private continuation first scrambles covered fields in separately prepared World/Engine owners, verifies immediate canonical bytes, then exercises ordinary cleanup/decay, scrub/daily redraw, refresh, ID/revision and cache publication. A small Round test child restores zero/saturated tallies and calls the unchanged ordinary notch/source-choice functions. Its module wiring is test-only; Round production code is unchanged.

New fixtures only: `world_marks.json` contains sparse IDs, all three kinds, raw strengths, historical/orphan references and disabled switches; `engine_marks.json` contains a spent daily beat, consumed allocation, clean empty store, original config and real chalk cache. Historical component fixtures and their README remain byte-identical to HEAD.

Two functional debug smokes (two samples per mode) exercise the actual authored 520-character world and +2,000 production placement (2,520). `alibi_marks_cost` emits the established six arrays and `marks-obligations-v1` metadata. Three deliberate live marks include both anchor variants, a weathered faint cross, saturated tally, ward sign and departed authors. An ordinary scrub preserves the same-day gate; an ordinary Engine poll publishes pen/nearby eligibility and the following poll proves dedupe. These are functional smoke checks; coordinator release repetitions establish cost evidence separately.

Authored/populated encoded sizes are 1,850/2,021 B, E is 29,932/33,020 B and per-cohort peaks are 4,323,678/4,336,543 B. Simultaneous Save+Load is 8,647,356/8,673,086 B excluding Running. Three-mark diagnostic occupancy is not maximum marks/registry/cache stress. It does not solve the earlier naive backbone+Round 1,256,093,444-byte reservation or existing synchronous frame-budget failures. Full integration must change phase/lifetime coordination and provide bounded host offload or incremental work, with no cap increase.

Retained development failures: `checkpoint_final` used a nonexistent LogicalTime::ZERO in a new test; `checkpoint_final_corrected` had a too-specific expected error phrase, while the valid duplicate key was correctly refused. Both are corrected and followed by passing checks. The initial three development commands preceded per-command source manifests and retain exact logs without a retrospective source claim; the final focused/layout/workspace verification is bound to the same frozen source. Renderer/GPU/full-stress and complete-envelope adoption were not attempted in this component cut.

Next coherent owner work: Dogs/body/path/counters, then independent ward moods/Night and the remaining Engine configuration/cadence, conversation/attention/speech/scheduler/pending obligations according to actual dependencies. All-consumer temporal/accounting/command horizons and complete World/Engine/host capture/hydration remain required.
