Status: Accepted as an existing component cut (2026-09-08); complete M2 remains pending.

# M2a7 coordinator review

World marks and Engine chalk components preserve existing marks, allocation order, processed clocks, World switches, original Engine configuration and the exact past chalk publication. Exact resolved catalog, registry and shelter bindings are checked before adoption. Decoding invokes no seed, constructor, sweep, scrub or public partial installation.

The six independent public tests pass: orphan marks survive until the saved sweep gate; a scrubbed empty store retains the spent daily beat and allocator; a calendar rewind followed by refresh retains age, author and identity; invalid decay settings retain their IEEE bits and ordinary fallback; changed catalog text refuses while exact definitions bind an unadopted backbone; and malformed input releases its lease while raw padding remains charged through candidate validation. Disabled World/per-kind switches survive and continue to govern reading after re-enable.

Private tests add strict nullable/unknown/duplicate records, sparse allocation and saturated eviction, nonempty scrubbed-component continuation, exact catalog documentation and duplicate anchor-slot binding, zero-stroke tally preservation, and ordinary Round increment/saturation after restore. Engine continuation scrambles every covered field, asserts immediate canonical equality and compares subsequent events, IDs, world revisions and cache publication. Cached handles may legitimately repeat or lag current World state. Original Engine configuration may differ from later public World settings; both copies are preserved.

The final frozen-source workspace passes **1,986 tests, 0 failures and 23 intentional ignores across 39 targets**, including all six public marks tests. Focused checkpoints pass 92 tests with 15 intentional ignores. The independent [log/archive audit](log_archive_audit.json) verifies all 12 Cargo records, four formatting/compiler archives, exact original bytes and per-command source bindings. The final focused/layout/workspace manifests are identical. Two corrected development failures and the first three commands’ lack of retrospective source manifests remain explicit in the owner record.

The independent [source audit](source_audit.json) verifies 831 frozen source hashes, all 15 changed source/fixture paths, 20 unchanged historical JSON fixtures and their README. Engine, marks and Round test files are byte-identical to HEAD after removing only new module wiring. Twelve changed Rust files pass scoped formatting; marks.rs retains independently verified baseline-only comment alignment drift. The source manifest SHA-256 is `383b1beef65f2e49dc0edac84aaf75f2f83e9506a4712fa4e90d5de75d462c63`.

The [admission proof](../ADMISSION.md) covers concrete serde records, sparse BTree roots, vector growth, retained raw-input charge and a 4 MiB context allowance. Mark records are 112 bytes; a sparse singleton BTree root is conservatively 1,488 bytes, below the mandatory record/anchor lexical charge. Registry validation scratch is bounded at 1,720,000 bytes and is dropped before later bounded catalog/shelter traversal. No borrowed definition parser or global cache allocates during candidate conversion.

The [release measurements](../performance/README.md) retain and independently audit six runs and **3,600 phase samples**. Authored/populated export p99 is **1.109896/4.411079 ms**, the median of three per-run percentiles; largest individual export is 4.802286 ms. Exact source, binary and runner hashes remain unchanged. The three-mark diagnostic preserves actual scrub/weathering/cache witnesses and places all 2,000 additional residents. Save+Load reservation is 8,647,356/8,673,086 bytes excluding Running. Historical Round/climate/knowledge/law datasets pass the amended auditor, and all three deliberately corrupted datasets fail their intended gates.

This is a component acceptance. Complete World/Engine/host composition, exact full behavior resolution, pending-work agreement, effective clock/accounting/allocation horizons and Running/retiring admission remain required. The existing backbone+Round Save+Load overage must be solved by actual phase lifetimes, and synchronous capture costs still require bounded offload or incremental host coordination. No complete-save, renderer or full-stress acceptance is added.

The next coherent cut is existing animal authority and its Engine publication state, followed by the remaining scheduler, conversation, speech, Night and pending-work dependencies.
