# M2a7 marks admission and closed layout — 2026-09-08

Status: Implemented component bound; coordinator acceptance pending. Global 1 GiB, payload/expanded 128 MiB and depth 64 caps are unchanged.

The existing nonallocating lexical preflight precedes extraction, typed parsing and context validation. Every object/array contributes 512 B, every scalar/string/key 64 B and every encoded string byte two more. For expanded charge E and original input bytes J, the attached peak is at least `4,096 + 4 × E + 3 × J + 4,194,304` B. The additional 4 MiB context allowance is admitted before bounded definition hashing/registry validation. Original whitespace and escape charges survive candidate conversion; no canonical re-encode shrinks a received reservation. Errors and cancellation release their leases; encoded Save bytes and the Load candidate each keep theirs until destruction.

The four expanded copies cover the concrete closed v1 records, collection capacity and temporary validation/deserializer work. Three encoded copies cover the input, escaped-string parser scratch (geometric capacity below twice input) and encoding work. These are supported-shape bounds, not claims for arbitrary Deserialize types. Owner prose has a 65,536-byte limit. Cached chalk labels allow that plus 1,024 bytes of fixed attribution text; handles retain the owner prose limit. Actor IDs have the existing 128-Unicode-scalar validity policy, guarded first by a sufficient 512-byte bound. Remote struct fields are mandatory; nullable fields use required direct adapters. MarkKind/Anchor/CalendarAnchor enum shapes are closed, and the streaming owner map rejects duplicate IDs before decoding a second value.

## Closed containers and sparse roots

The retained layout diagnostic reports Marks 56 B, Mark 112 B, MarkAnchor 32 B, switches 3 B, World DTO/candidate 224 B, Engine DTO/candidate 504 B, EngineMessage/Option<EngineMessage> 232 B and ChalkableHere 72 B. The Marks map uses the strict direct streaming BTree adapter, without an intermediate row Vec. A minimal full Mark receives both mark/anchor object charges plus all eight mandatory field-key/value charges, even for empty strings and nullable references: two objects (1,024 B), eight outer keys (512 B), and nine scalar/string values or anchor-key contributions (576 B) already total at least 2,112 B before string-byte charges. This covers the sparse singleton BTree root at `11 × (8 + 112) + 168 = 1,488` B, not just an assumed fully occupied tree. At most 100 live rows are supported. Duplicate `(kind,anchor)` records remain legitimate low-level insertion state and do not create extra validation indexes.

ChalkableHere contains three 24-byte String/Vec headers on this target. Its Vec first allocation floor is at most four 72-byte rows (288 B), then geometric capacity below twice populated rows; the array and record charges cover both. A kind Vec contains at most three unit enums. The decoder may temporarily read more before semantic refusal, but their scalar/array lexical charge was admitted first. Cache row count is bounded by 25,000; repeated handles are not duplicate map keys and deliberately remain distinct ordered rows. No cache reference-resolution set or current-world rebuilding is allocated.

## Borrowed definitions and validation scratch

Context comes from a live World or an opaque unadopted backbone and caller-supplied MarkCatalog/ShelterMap. Character and registry entry counts are each capped at 25,000. The complete ordered registry/home wire streams through a 4 MiB byte ceiling before pure index/finite-point/identity/home-owner validation. On this target, registry name validation's `BTreeMap<(&String, usize)>` uses eleven 16-byte slots plus 168 B per node. A root with at least one key and other nodes with at least five allows at most `1 + floor((25,000 - 1) / 5) = 5,000` nodes, or 1,720,000 B. It is dropped before further context validation; the 4 MiB allowance remains attached, leaving headroom for bounded hash/serializer/error state.

Resolved catalog validation traverses exactly three specs. Each ordered anchor vector is capped at 1,024; each place map at 25,000, with bounded strings and a 4 MiB streamed fingerprint ceiling. All five numeric fields are checked finite and consistent with the ordinary catalog's thresholds/half-life relations before hashing. Duplicate anchor slots remain allowed. Full retained documentation and unreferenced map entries are fingerprinted, so equal IDs alone cannot substitute another catalog. No trusted-content parser or default constructor allocates during candidate conversion.

Shelters are bounded at 256, each polygon at 1,024 points and office vector at 1,024. IDs, labels, finite points/spread are checked before the bounded full-record fingerprint. Shelter and catalog borrowed iteration allocates no per-row scratch. Invalid registry floats may be serialized as null into a temporary digest, but the digest cannot become a successful binding before the existing registry validator rejects them. No filesystem, clock, network, threads, navigation bake or process-global cache access occurs in simulation production checkpoint code.

## Supported numeric policy and remaining composition

Both processed clocks preserve CalendarAnchorV1 Never or signed finite days within the existing ±1,000,000-day format. Drawn/decayed dates are independently bounded; no chronological ordering with each other or current calendar is invented. Strength is finite in [0,1]. A tally permits 0..=12 (source explicitly renders an abandoned zero-notch mark); other kinds keep one stroke. The public Round test child proves ordinary increment and saturation after restore.

The mark allocator requires 257 advances of headroom (`MAX_STEPS + 1`). A private test restores the last supported value, performs exactly that many ordinary capped inserts while checking faintest/lowest-ID eviction, and refuses a later capture once the headroom is consumed. This is an explicit component horizon, not proof of arbitrary future command vectors, catch-up bursts or city lifetime. The world revision is separate backbone authority and must agree during complete composition. Exact compiled hash/behavior manifest and all-consumer temporal/accounting horizons remain M2b obligations.

The scale fields preserve IEEE bits including externally reachable invalid configurations. Ordinary fallback remains the authority; decode does not sanitize to 1.0. Finite positive scale multiplication may overflow elapsed decay to positive infinity, which ordinary powf maps to zero strength/removal; this does not certify future clock-rate CPU cost. Saved climate/current precipitation and the rest of the ordinary writers must still compose from their own validated owners.

## Diagnostic phases

`alibi_marks_cost --mode authored|populated --samples N --output PATH` supplies the established six raw timing arrays. Preflight binds/hashes definitions and counts the borrowed full component. Export repeats bounded checks and clones the exact Marks, switches, initial Engine config and publication. Encode writes canonical bytes. Decode includes raw admission, typed/index construction and reference/context/cache validation. Candidate validation repeats pure checks and moves the DTO; drop releases candidate and save bytes. All temporary working allowances remain retained in their leases.

The `marks-obligations-v1` workload uses actual authored and +2,000 production placement, a spent ward day after an ordinary scrub, a second historical-author cross weathered to a faint 0.2143109957132682, a saturated tally and a ward sign at actual registry anchors. An ordinary poll publishes a real nearby chalk eligibility cache with a held pen; the next poll proves dedupe. No cleaner worker is invented. The tally count is deliberately set through the public mark field in probe setup; separate private Round tests exercise the ordinary increment. History subjects/orphan anchors and maximum occupancy are covered by deterministic tests, not claimed as this three-mark workload. Debug smokes are functional evidence only; coordinator release repetitions and independent auditing remain separate.

No cap is increased to make complete capture fit. Naive backbone+Round Save+Load already exceeds 1 GiB without Running, and previous export phases exceed the 2 ms frame budget. Future integration must solve phase/lifetime admission and bounded offload/incremental host work.
