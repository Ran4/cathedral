# M2a9 existing Night and independent ward moods — 2026-09-08

Status: Implemented, independently reviewed and accepted (2026-09-08). Component candidates only; complete M2/M3 remain pending.

NightOfficeDtoV1, WorldNightDtoV1 and EngineNightDtoV1 preserve the real Night owner, independent exact World ward moods and original Engine Night config. Queued admission identity/day/incarnation, exact flight prompt/request/root and held success/error/duration remain obligations. Queue-time guards are never treated as completions. Read-only candidates support borrowed World or unadopted backbone/ledger context with exact bitwise clock binding; no public partial install or constructor reseeding exists.

[Owner coverage](OWNER_COVERAGE.md) lists every field, historical reference and remaining adoption dependency. [Admission](ADMISSION.md) proves closed records and sequential scratch under unchanged 1 GiB/128 MiB/depth64 caps. [Commands](commands.json) retains exact raw stdout+stderr identity, environment and source at each command start, including development failures. [Compiler/environment](environment.json) records the fixed offline/headless build settings.

Two new component fixtures retain live engine queued/submitted authority and exact public ward mood values. The old 24 JSON fixtures and README remain unchanged. These cannot load an entire saved city. Private tests use independently prepared dependencies, scramble covered owners and require immediate canonical equality before ordinary continuation. They cover held capacity deferral/release-once, error detail, stale/obsolete duties, Busy pacing, newer queue-time stamps, real ambient once/day work, ward mood/mark/pending round edit/receipts and Engine polling. Independent public boundaries cover arbitrary mood text, queued history, exact prompt, old incarnation/root binding, positive-infinite pacing and strict/raw admission.

Both two-sample debug probes pass on actual authored 520 and +2,000 placed 2,520 actors with real navigation. At 20.200000000000003 seconds they retain six queued ward duties, a second submitted ward request with its exact held success, thirty resolved bedtimes, eight daily stamps and one applied 39-byte ward mood. Three provider attempts include one Busy refusal and two submissions; the first reflection completed, while the second is held by an actual 4,096-recent/256-retained receipt window. The probe observes ordinary ambient reroll; once/day correctness is a separate real-resident continuation test.

Both modes encode 7,480 B, charge E 52,832 B plus 4,194,304 B working allowance and retain 4,432,168 B per cohort. Save+Load is 8,864,336 B excluding Running. Current flight prompt is 4,987 B; maximum of the two actual submitted prompts is 5,301 B, below the supported 64 KiB capture policy. The standard six timing arrays use night-obligations-v1. Release repetitions and final acceptance are coordinator-owned; debug timings are functional witnesses only.

Complete pending-work adoption remains M2c: exact held success/error once, one load-specific retry for unfinished exact inputs inside the restored valid window, and old-callback fencing. All-consumer horizons, remaining owners/envelope, accepted host/player samples/residuals, initial publication, capture/hydration and phase/lifetime coordination remain pending. Prior backbone+Round naive Save+Load already totals 1,256,093,444 B before Running, above 1 GiB. No cap increases, synchronous host path or renderer acceptance are claimed.


The [source freeze](source_freeze.json) pins an explicit [852-file source/assets map](source_hashes.json), SHA-256 fa65084e5e2a2509ec742af39407047dace120badda9176962c638c98f3d6fc3. [Scope audit](development/source_scope_audit.json) verifies byte-identical ordinary Night/Engine/traits code after removing only module wiring, unchanged prefixes in the existing clock/ledger checkpoint modules and all 24 historical JSON fixtures plus README. The coordinator separately pins default_config.ron, a runtime-read test input outside the inherited manifest's explicit source/assets scope. Final focused checkpoints pass 119 tests, zero failures and 21 intentional ignores against this manifest; all six independent public tests and the layout diagnostic also pass against the same manifest. The successful serial full workspace retry passes 2,025 tests, zero failures and 29 intentional ignores across 41 test-result targets against the same manifest; [verification](verification.json) records the exact command and totals.

All development failures are retained rather than replaced: initial check found import paths and the missing numeric RequestId adapter; initial private compilation found test asset-relative paths and a nonexistent WorldClock::default; the first public run found a test-only retained LoadCandidate lease before a second reservation. Fixes preserve the assertions and existing ordinary behavior. The first serial workspace run then exited 101 during linking as the filesystem filled. Its exact 4,285 captured bytes are retained; the linker diagnostic was cut off by disk exhaustion, so unavailable output cannot be recovered by archiving. After disk recovery, workspace_retry exposed a previously cached dependency's build-script assumption: libudev-sys directly launches rustc, but the recorded PATH omitted its directory despite the absolute RUSTC setting. Its complete 16,356 captured bytes are retained. workspace_path_retry passed the full serial workspace with /home/ran/.cargo/bin appended to PATH; the compiler, source, dependencies and other build settings are unchanged. The [environment record](environment.json) pins both PATH values and the exact dependency source/excerpt. No already-passing focused/public/layout checks were repeated.


The final layout is NightOffice 304 B, Due 72 B, Flight 96 B, Subject 24 B, Completion 64 B, CognitionError 48 B and Config 4 B; World/Night/Engine DTOs are 40/368/424 B. Borrowed Subject and OperationId scratch records are 8/16 B. Closed sparse-map and deque bounds fit the unchanged aggregate charge and retained 4 MiB working allowance. The detailed proof distinguishes byte admission from actual allocator/RSS and host frame behavior.

[Coordinator acceptance](coordinator/review.md) verifies the unchanged-source workspace retry (2,025 passed, 29 intentional ignores), all retained original logs, source and admission evidence, and [3,600 release phase samples](performance/README.md). Authored/populated export p99 is 0.048061/0.036078 ms, the median of three per-run percentiles. The amended auditor also passes all six historical component datasets and rejects the three deliberately corrupted Night datasets. This accepts this component without establishing complete-save or host-frame performance.
