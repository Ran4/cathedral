Status: Accepted existing component scope — 2026-09-08. Complete M2/M3 remain pending.

# M2a9 coordinator review

The existing Night Office now has an opaque admitted checkpoint for exact queued duties, queue-time daily stamps, resolved bedtimes, submitted request/prompt/root/incarnation, held completion, office and ambient guards, pacing and counters. World ward moods and original Engine Night configuration are retained separately. Decoding calls no constructor, seed, prompt renderer, ordinary poll or production adoption path. Exact live clock segments and borrowed semantic roots bind the component; full owner-set agreement remains the envelope's responsibility.

All six independent public tests pass. They preserve arbitrary public ward text (including empty, blank and more than 600 Unicode characters), queue-time stamps after ordinary repeated ringing, Busy admission through an ordinary departure with the old incarnation retained, exact submitted prompt bytes after live intent changes, and an overflowed historical pacing deadline after the live clock returns to its admitted range. They distinguish different clock segments, require the saved ledger root, preserve original Engine flags and reject malformed records. One MiB of raw padding retains at least three MiB of additional reservation through candidate validation. The first Busy test run exposed a test-only double LoadCandidate lease; an explicit drop corrected its scope without changing assertions.

Fifteen new private/fixture tests pass. Independently prepared owners are deliberately scrambled and restored to immediate canonical equality before ordinary continuation. Receipt-window saturation retains an actual successful completion; releasing capacity applies it once with identical events and receipts. Exact error kind/detail, stale incarnations, obsolete queued days, Busy retry identity and the changed-clock pacing edge remain intact. A newer queued day can coexist with its subject's older unfinished flight; same-day duplication and impossible simultaneous admissions are rejected. Ward replies preserve mood, chalk mark, pending Round edit and four action receipts. A real seeded ambient fixture saves the daily guard before restoration, uses a cleared-guard positive control that demonstrably rerolls, and proves same-day suppression followed by matching next-day changes. Other cases cover disabled/unseeded history, unadopted backbone/ledger borrowing, signed-zero clock rejection, negative-zero pacing, full historical-map/queue shapes and ordinary Engine polls. The two new fixtures leave all historical fixtures untouched.

The successful serial [workspace retry](../verification.json) passes **2,025 tests, zero failures and 29 intentional ignores across 41 test targets** in 1668.72 seconds, against the unchanged frozen source. The final focused checkpoint run passes 119 tests, the independent public boundary passes six, and the layout diagnostic passes one. The [independent archive audit](log_archive_audit.json) checks every original/archive hash and byte length, parses all test totals and verifies the source manifest recorded at each command start. All five failures in that command record remain archived: the two initial compile failures, the corrected test-only lease scope, the first workspace link failure during disk exhaustion, and the clean rebuild exposing a libudev build script that required rustc on PATH. The [compiler-path audit](compiler_path_audit.json) verifies that PATH now resolves the same pinned compiler. The original linker diagnostic was cut off; it is retained exactly rather than reconstructed. The unchanged-source retry followed recovery of disk space and appending the already pinned compiler directory to PATH; no source or dependency edit was needed.

The independent [source audit](source_audit.json) verifies all **852 frozen compiled source/assets files**, all sixteen changed source/fixture paths and all 24 unchanged historical JSON fixtures plus their README. Fourteen changed Rust files pass scoped formatting. Removing the checkpoint module declarations makes ordinary Night, Engine and backend-trait source byte-identical to base commit `c9f5b39`. The existing clock and ledger checkpoint files are unchanged before narrow appended read-only helpers. The clock helper compares every descriptor float by bits; the ledger helpers borrow existing roots or receipts. Frozen manifest SHA-256: `fa65084e5e2a2509ec742af39407047dace120badda9176962c638c98f3d6fc3`. A [supplemental audit](runtime_config_audit.json) pins the unchanged committed root-level defaults read at runtime by game configuration tests, outside the inherited manifest's explicit assets/lore scope. The [release archive and scope audit](release_archive_audit.json) reconciles the separate 876-path measurement identity: 850 shared hashes agree, two additional lore/places JSON inputs retain the frozen hashes, and 26 measurement-only text fixtures/documents remain identical to HEAD and the earlier committed M2a8 identity.

The [admission proof](../ADMISSION.md) binds NightOffice (304 B), Due (72 B), Flight (96 B), Subject (24 B), Completion (64 B), CognitionError (48 B), World DTO (40 B), Night DTO (368 B) and Engine DTO (424 B) layouts. Sparse tree nodes, VecDeque growth and exact string clones fit the aggregate four-E allowance. The attached 4 MiB working charge covers sequential saved-ledger validation sets and bounded borrowed Night indexes; the queue order is never changed by validation. Shape limits admit 25,008 duties/stamps and 25,000 historical bedtimes subject to aggregate caps. Prompts and individual mood/error fields keep their explicit byte bounds; public moods retain the general text bound. Dropped counters reserve 50,017 increments for one ring/poll's old and newly queued duties plus one harvested flight; reflected reserves one. Positive-infinite pacing is retained as Never and finite historical futures are preserved exactly. These are local admission bounds, not an all-future arithmetic or allocator measurement claim. The [supplemental allocation audit](stdlib_allocation_audit.json) independently checks the exact installed standard-library source hashes and line excerpts underlying deque clone/growth, empty-map clone and sparse-node assumptions.

The [release measurements](../performance/README.md) independently audit six runs and **3,600 raw phase samples** on one unchanged executable. Authored/populated export p99 is **0.048061/0.036078 ms**, the median of three per-run percentiles; maximum individual export is 0.138482 ms. All runs retain queued ward duties, a submitted exact prompt and capacity-held reply, prior settled mood and witnessed Busy/application history. Save+Load reservation is 8,864,336 bytes excluding Running. All six historical component datasets pass the amended auditor, and three deliberately corrupted datasets fail the intended percentile, allowance and held-result gates.

This accepts only the existing component. Full pending-work adoption must consume a saved completed success/error once without a new provider request, and perform exactly one load-specific retry for unfinished submitted work with the saved input, owed identity and generation fence. Ordinary stale-incarnation and obsolete-day checks still apply in their normal paths. Historical references and valid queued work must not be cleaned up during decoding.

Complete World/Engine/host assembly, all-consumer clock/counter horizons, manifest/build/target/toolchain resolution, initial host publications and actual Running/Save/Load/retiring lifetimes remain pending. The naive backbone+Round Save+Load reservation already exceeds the unchanged 1 GiB shared cap. Full capture must also address the previously measured components that exceed synchronous host budgets. Remaining Engine configuration/cadence, conversation/attention/scheduler/speech and external work need their own owner cuts. This adds no complete M2, renderer or full-stress acceptance.
