# M2a9 Night admission — 2026-09-08

Status: Implemented and independently reviewed with frozen layout, focused/public/workspace verification and release measurements. Unchanged global ceilings: 1 GiB shared, 128 MiB encoded and expanded per component, depth 64.

The nonallocating lexical meter sees the borrowed full component before cloning and the entire raw input before typed decode. A cohort retains at least `4,096 + 4*E + 3*J + 4,194,304` bytes, where E counts 512 bytes per object/array, 64 per key/scalar/string plus twice encoded string bytes, and J includes original padding and escapes. Raw charges stay attached through candidate validation; no canonical shrink occurs. Error/disposal releases the lease. Preflight and encoding stream serde records directly, never through serde_json::Value.

The 4 MiB owner allowance covers sequential borrowed context and reference validation. Saved-ledger validation temporarily constructs three bounded BTreeSets (4,352 CommandIds, 4,352 ordinals and 256 roots), then drops them before Night scratch. With 11 slots/node, at least five populated slots per nonroot and 64-byte header plus twelve 8-byte child pointers, their combined upper bound is below 650 KiB plus bounded 1 KiB per-entry serialization work. Night then temporarily holds sorted Vec<&Subject> and Vec<OperationId> for at most 25,008 queue rows plus one flight. Conservative doubling capacities use less than 400,128 and 800,288 bytes respectively. Sorting does not allocate or reorder the queue. These scratch lifetimes fit within 4 MiB including fixed serializer/errors/context; no registry/geometry/World clone or Round/catalog parser runs.

Closed records preserve direct ownership. Subject uses an externally tagged strict enum. Due, Flight, Config, Night, World and Engine records reject unknown/duplicate fields; all nullable fields are required. Queue parses directly into VecDeque with no intermediate Value/Vec. Validation enforces unique subjects, exact queue-time stamps, at most one admitted front row without a flight, no same-subject/day queued/flight duplication and a corresponding queued row for a changed flight stamp. Daily stamps parse closed {subject,day} rows into a unique BTreeMap; bedtimes and moods reject duplicate keys. CognitionError/Completion reconstruct exact private fields through owner remote records. Success text is bounded at 400,000 bytes before parser allocation and additionally 100,000 Unicode scalars before candidate admission; error kind/detail each use the existing 65,536-byte text policy. Ordinary held successes already passed this provider-reply check. Errors are preserved without truncation.

The supported prompt policy remains the existing 64 KiB prompt canary (65,536 bytes), and capture refuses larger exact prompts rather than truncating or re-rendering. Authored/+2,000 production workloads measure actual submitted prompts; they are smaller than this admitted policy, while arbitrary public state can render larger unsupported prompts and must fail capture explicitly. World mood public edits use the general 65,536-byte text limit, not the ordinary writer's 600-character cap. They may be empty or whitespace. All independent text fields remain additionally subject to combined E/J limits.

For sparse BTree maps, count the first leaf at full eleven-pair allocation, and subsequent nodes at a conservative `(n-1)/5+1` bound including child pointers. Decoded empty maps begin without nodes, and the pinned standard-library clone explicitly returns a new empty map when is_empty(), regardless of a formerly retained runtime root. Stamps encode each (Subject,i64) as a closed object plus nested enum; the 512-byte outer list and at least 1,344 E per row cover the full sparse first leaf and per-node growth, in addition to owned strings. Bedtimes encode ID key and Office string value; the outer object contributes 512 E and every pair contributes at least 128 E plus encoded bytes. For (ActorId,Office), the sparse singleton leaf is at most 512 E; for later sparse nodes the node cost per entry remains below 128 E. Moods have (PlanningWard,String) pairs with the same sparse-node envelope, and string contents separately charged. DTO parsing, exact clones and candidate moves fit the four-E cohort allowance.

Each Due row has 512 E plus four keys, scalar day and mandatory Option/Subject records; nonempty roots and Subject objects add their own charges. Each Flight has six keys plus exact prompt, Subject, incarnation, root and numeric request; inline Completion retains its result/error and duration. The outer Night record contributes thirteen keys and its own object before any collections. VecDeque/Vec geometrical growth and four-element singleton floors are covered by row/container charges; final layout diagnostics pin concrete values. Shape tests cover 25,000 historical people plus all eight wards, 25,000 resolved bedtimes and sparse maps. These are component maxima under aggregate caps, not timed-world counts.

Only next-poll counter headroom is established: 50,017 dropped and one reflected increment remain available. Office catch-up allocation, effective clock-rate CPU safety, future bounded command horizons and all consumers remain full-envelope gates. Pacing preserves reachable +Infinity as Never; finite At anchors include signed zero and arbitrarily distant nonnegative finite historical deadlines. These dormant obligations are not rescheduled by decode.

The six probe phases are borrowed preflight, exact export, encoding, raw decode/validation, repeated candidate validation and disposal of both cohorts. The probe uses actual authored 520/+2,000 placed 2,520 Engines, real nav/definitions, ordinary first Busy admission, one applied ward mood and a second completion held after a genuinely saturated protected receipt window. Max shapes, error terminals, stale incarnations, mark/round edits and extreme clocks are separate deterministic tests. This small workload is not full-save, maximum-shape, host-frame or renderer acceptance. No cap increases; complete phase/lifetime coordination remains mandatory.

Both debug smokes admit J=7,480 B, E=52,832 B and per-cohort peak=4,432,168 B; Save+Load=8,864,336 B excluding Running. The held prompt is 4,987 bytes; the maximum of two actual submitted ward prompts is 5,301 bytes in both 520/2,520 worlds. These two prompts justify the timed workload against the 65,536-byte supported input policy; they are not a maximum over every possible authored/public state.


Frozen x86_64/rustc 1.96.0 layout output: NightOffice 304 B, Due 72 B, Flight 96 B, Subject 24 B, Completion 64 B, CognitionError 48 B, Config 4 B, WorldNightDto 40 B, NightOfficeDto 368 B, EngineNightDto 424 B, borrowed Subject pointer 8 B, OperationId 16 B. Each candidate contains only its already admitted DTO and moves it without building additional indexes. A Due's four-slot first deque allocation is 288 B and geometric growth stays below twice populated rows; one minimally populated Due contributes at least 1,600 E. Flight and Completion fit inside their closed record/object/string charges. Night's thirteen keys plus outer object already contribute 1,344 E before values and collections, covering its 304-byte owner inline layout. Subject/ActorId plus i64/Office pairs fit the conservative 32-byte map-pair slot; a full sparse leaf allowance is 512 B, already covered by outer-map and row charges. The separate 4 MiB working allowance remains conservative and retained even after sequential scratch drops.

[Installed standard-library allocation source](development/stdlib_allocation_audit.json), tied to the recorded rustc --print sysroot result, confirms VecDeque::clone reserves its populated len rather than historical queue capacity, empty BTreeMap::clone returns a new map, nodes use eleven slots/minimum five, and RawVec growth doubles with a four-slot floor for these record sizes. Source SHA-256 and exact line excerpts are retained as supplemental proof; no Rust repository files changed.
