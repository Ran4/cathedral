Status: M2c implemented and reviewed (2026-09-15). M2d continuation and M3 application adoption remain.

# M2c — Preserve pending obligations

Joint preparation now transforms quarantined hydrated owners into a quarantined
prepared continuation. Exact unfinished cognition remains owed independently of
newer protected work, with per-obligation knowledge context. Night preserves
day/incarnation authority and exact accepted input; inactive saved executions
cannot consume reused request IDs. Unfinished recordings become durable unsent
history with their original terminalized receipt notifications. Existing readable
Floor pacing survives after obsolete microphone/audio liveness is cleared.

Complete capture supports every prepared state through strict component V2
extensions, including an old deferred prompt beside a newer accepted prompt and
immediate second preparation/re-save. Legacy V1 fixtures/readers remain intact.
Explicitly admitted service binding installs generation-scoped handles without
calling service traits. Production Engine/World access and host adoption remain
quarantined for M3.

| Evidence | Scope |
| --- | --- |
| [Coordinator acceptance](coordinator/review.md) | Independent source, command, performance and fixture audits |
| [Release measurements](performance/README.md) | 600 preparations, all cold/slow samples and exact memory bounds |
| [Fixture audit](coordinator/fixture-audit.json) | Six identical fresh writers and two exact fresh readers |
| [Fixture publication](coordinator/fixture-publication.json) | Only two verified outputs and README added after the source freeze |
| [Owner design](owner-design.md) | Exact retries, contexts, speech receipts, Floor and schemas |
| [Admission](ADMISSION.md) | Unchanged budgets, structural growth and destruction order |
| [Coverage](coverage.md) | Independent public/behavior suites, complete joint case and relocated legacy checks |
| [Owner handoff](OWNER_HANDOFF.md) | Final source/binary identities, verification outcomes, Cargo cession and remaining boundaries |
| [Verification record](verification.json) | Audited command archives, full workspace totals and debug probe identities |
| [Owner command records](owner/) | Every original command, start-source/environment identity and exact gzip log |

The owner runner preserves failed/development attempts. `check-01` exposed
mechanical visibility/type errors; `continuation-01` exposed test-only ID
constructors, an old explicit struct literal and one misplaced validator guard.
`public-01` was a superseded mixed-source development build whose library
predated the newly required queued-incarnation extension. `public-02` passes all
seven independent public host cases. `continuation-02` passed31 cases and exposed
two root fixture preconditions, corrected without weakening assertions.
`sim-lib-01` passes822 cases, including all six corrected root behavior cases;
its sole failure was the new synthetic complete fixture missing required HUD
records before initial capture. All failures remain archived. Superseded or
mutating development attempts retain their original `sources_unchanged=false`
records and are not final-source acceptance evidence.
`probe-authored-01` failed before continuation in readable-workload setup: its
probe-only idle="all" startup override differed from M2b's normal stage setup,
allowing unrelated initial work to consume the warmup's short pacing window.
The override was removed; the ordinary subtitle assertion remains unchanged.
The unchanged-source rerun `probe-authored-02` and populated probe both pass
with actual unfinished cognition, one accepted recording, eight exact unaffected
categories and exact full bytes after a second preparation. Their original
reports are preserved under `owner/`; release distributions remain coordinator
evidence. `format-check-01` passes all39 changed/new Rust files against the same
980-input source map as those successful probes.
`legacy-integration-01/02` exposed two test-fixture assumptions after the explicit
legacy migration: ward budget constants and the difference between the historical
fully initialized Engine prompt and this Night-only world's prompt. Both are
corrected and labeled; `legacy-integration-03` passes all12 integration cases.
`format-check-02` passes the final39-file source set. `workspace-01` passes
**2,215 tests, zero failures, 44 intentional ignores across46 groups**, including
all seven independent public cases, six independent behavior cases, the full
mixed-context envelope regression and evicted-receipt ordinal regression. The
frozen980-input map is [source_hashes.json](source_hashes.json), SHA
`b277379757dd70a1476adb44e583aeb808533ef58178fa88f70f5e20600d35a1`.
All23 owner commands have exited and their original logs are sealed. Cargo
ownership has explicitly passed to the coordinator for release verification.

All commands are offline, serial Cargo, hidden/fake host only. The actual-city
probe distinguishes preparation, inert binding and prepared-owner disposal;
source-host retirement, device construction, frame placement and complete M3
adoption are outside its measurement. Broad future continuation remains M2d.

Final acceptance uses unchanged 980-input source map
`b277379757dd70a1476adb44e583aeb808533ef58178fa88f70f5e20600d35a1`.
The final workspace passes 2,215 tests with zero failures and 44 intentional
ignores across 46 groups; all 39 changed/new Rust files pass formatting.
The independent command audit verifies all 23 attempts and their exact archives.

The release audit retains 600 samples, comprising 1,800 complete-phase and
3,000 stage timings. Both measured source shapes contain one unfinished
scheduler request and one accepted recording. Every sample preserves its exact
retry, interruption and eight unaffected categories; each process also passes
complete second-load byte equality. Fresh fixed-lineage writers/readers produce
the two published continuation fixtures with exact original-input equality.
No M3 host-frame, real-service construction or whole-host retirement claim
follows from this preparation evidence.
