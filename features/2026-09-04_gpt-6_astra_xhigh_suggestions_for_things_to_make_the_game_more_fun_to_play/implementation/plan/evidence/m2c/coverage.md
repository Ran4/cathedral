Status: M2c coverage verified and reviewed (2026-09-15). M2d and M3 boundaries remain.

# M2c coverage and remaining boundaries

The final `workspace-01` run passes2,215 tests with zero failures and44 intentional
ignores across46 groups. The sim library contributes824 passed/28 ignored; the
host test binary contributes602 passed/11 ignored. All described non-ignored
M2c cases pass in this frozen-source run; the separately invoked authored and
populated debug probes also pass. [Verification](verification.json) preserves
their distinct source scopes and every earlier development failure.

The root-owned public host suite observes the admitted complete pipeline without
an Engine/World extraction. Seven cases cover unaffected categories, exact Host
boundary/time/lineage, stage failures and unwinding, shared pressure, same-budget
observations, generation binding, every service-trait panic sentinel and service
destruction under its lease. Actual accepted recording preparation advances the
original receipt once, releases its root, and preserves exact second-save/load
bytes. Required/null/unknown V2 extensions and contradictory/duplicate
interruption provenance refuse at their actual owner boundary.
An additional owner case normally evicts two real accepted/interrupted receipt
identities, proves each history is individually valid, then rejects contradictory
shared ordinals across groups and within one group. It also rejects an archived
ordinal already owned by a retained command from a different family, through
both live and saved ledger projections.

The six root-owned simulation cases use one explicitly cfg(test)-only adoption
stand-in. They cover offstage/empty-inbox exact retries, new protected work while
the old input remains deferred, Busy and round-robin conservation, original
presented-history graduation, and two knowledge contexts including a newer
same-actor occasion and application-created effects. Held success/error/oversized
completions apply once without another request. Night rejects obsolete day and
incarnation work; an inactive saved Night flight cannot harvest a reused numeric
scheduler ID. Every old/unwrapped callback family is fenced, with current
generation positive controls.

Owner cases separately cover full retry saturation/backpressure and composing
suppression, actual subsequent provider failure, exact Night method/budget/Busy,
lazy gate computation with an empty queue, held Night completions, queue-time
incarnation persistence, explicit V1 migration and its counter headroom edge.
Floor checks retained foreground/background readable pacing, original failsafe
caps, missing-readable release and repeated preparation without a fresh beat.
The closed structural layout proof includes queue/vector growth and receipt
advance/drain working storage beneath the unchanged preparation allowance.

The pre-existing `checkpoint_night_boundary` integration cases now distinguish
ordinary person queues (which explicitly refuse insufficient V1 export) from
historical V1 codec inputs. Variants of the unchanged historical Night fixture
retain their decode/re-encode, Busy receipt-root, exact prompt, clock-segment and
overflow-pace checks; ordinary owed/slot/totals observations remain. Actual new
queue serialization/incarnation assertions live in the current complete V2
owner and continuation suites. Two cognition-input component-composition cases
use V1-compatible ward queues, while their existing separate exact-input matrix
still exercises both person and ward requests. No public candidate extraction
or cloning API is introduced for these tests.

`engine::continuation::complete_tests` fills the full-envelope joint-validation
case: ordinary old and newer same-actor prompts both retain their distinct
seated facts across complete capture/validate/hydrate/prepare/capture and a
second load. It also corrupts deferred Knowledge allocation authority and removes
the original protected semantic root. Its explicit synthetic Host fixture and
diagnostic JSON copies test behavior and joint validation; they are not heap
measurements or production host adoption.

The ignored actual-city probe measures separate preparation, inert service
binding and prepared-owner disposal boundaries for authored520/+2000 placed
residents, with ordinary pending cognition, accepted recording and readable
work. Its source-shape report distinguishes held and unfinished requests rather
than inferring one from a shared count. Eight unaffected categories compare
exactly per sample; first-sample V2 re-save/load compares complete bytes. Optional
fixture writer/reader modes report original-input, emitted-fixture and repeated
save hashes and preserve image binding.

M2d still owns the broad deterministic future-continuation matrix. M3 still owns
host-time binding, inactive ECS/controller staging, publication of owed receipt
notifications, service runtime/job ownership, current-load supersession,
authoritative swap and retirement. No public prepared Engine borrow/poll,
filesystem/device work in cathedral-sim, unsafe Send, renderer probe or
20,000-resident run is introduced by this leg.
