# M2a2 owner coverage — 2026-09-08

Status: Implemented and reviewed. Private character/inventory/backbone components; complete M2a remains pending.

M2a1 is accepted at `72ed9be9115ffd2c899fe3da38a58252ec856725`. Its archived owner coverage and measurements remain historical evidence. This ledger records the additional component cut, not a complete World payload.

| Owner | Preserved authority and local gate |
|---|---|
| Character identity and seed sheet | Every `CharacterSheet` field: identity/name/control, story/location/voice, full appearance and bespoke palette, seed pose/facing, historical holds/pockets/body-slot override, goal/memories/known identities, full lore including relationships/home/description/curiosity/generated routine, seed presence/epoch/economic class. The live `CharacterState` is preserved separately, never constructed by `from_sheet`. Historical item references can name consumed/transferred items. |
| Character live private state | Exact pose/facing, ordered holds/pockets/gut, urgency anchor/debug override, needs/statuses, goal/memories, perspective actor/place sets, inbox/recent/pending ordered occurrences, movement/polyline/speed/gait/choke/patrol, travel target/budget/deadline/receipt, round edit/epoch/teaching flag/index/target, daily-round prose, resident projection, vendor list, gesture/deadline, leaving-city flag, live presence/epoch/economic class. Mandatory nullable fields never default an omission to None. |
| Live item/quantity authority | Full ordered-ID item map with kind/quantity/exact metadata, offers with giver/target/event sequence/quantity, legacy restock shares, active transform producer/job/spec/day/slot/ordered inputs/output plans/progress, held completion job/producer/ordered consumed/produced/day lineage. Every live item has exactly one current holder. Pockets + offers + transform inputs are summed with checked arithmetic; duplicate input/output lines remain legal. Global job identity is unique across active producers and completed records. The production admission path now rejects a second producer's duplicate active job ID before mutation. |
| Immutable item catalog | SHA-256 domain `cathedral-item-catalog-checkpoint-v1\0` over the exact schema version and sorted full catalog definitions. Decode requires the supplied catalog hash to match; known kinds, metadata domains, stackability and quantities are checked. No embedded-catalog fallback on mismatch. |
| PlaceRegistry | Save ordered entries and exact home owner/ID bindings. Rebuild all four lookup maps. Ordinary duplicate display names keep the earliest normal entry; homes remain excluded from name lookup and may share names. Stored IDs are not re-minted through `add_home`/hash collision search. Validate every live home owner against the candidate roster. |
| World reference backbone | Save character map and insertion roster, composed inventory, places, household door bindings, round/travel command maps, world/event/spatial counters, sounds/view-cone configuration, sampled WorldTime, Needle claim and same-turn speech marker. Reject unflushed DomainEvents and ledger dispatch/notification buffers at export. This is named `WorldBackboneDtoV1`, never a complete World save. |
| Legacy undertaking bindings | Live map entries require exact actor/state receipt, current presence, matching edit incarnation, protected nonterminal ledger receipt and unique command. Historical receipt tags remaining in an old intent need not own an active map entry. Pending target/deadline/edit validation respects ordinary Round cadence: overdue work or an unavailable attempted target is preserved for its next normal decision. Full command-root accounting still awaits all semantic owners. |

The current gut owner stores **kind, metadata and due calendar time**, not the swallowed object's exact ID. The DTO preserves this actual state. A future richer physical lineage model belongs to M7; it is not fabricated during checkpointing.

Entity validity is the city's `MAX_ID_CHARS=128` Unicode scalar policy (up to 512 UTF-8 bytes), followed by the existing no-control-character check. The receipt subsystem's 64-byte principal-reference retention rule does not redefine entity identity. Live knowledge sets resolve against candidate actors/places; seed/history/attempted references follow their distinct historical policy.

## Required later owner contracts

| Pending owner/composition | Exact remaining contract |
|---|---|
| Round/residents (proposed M2a3) | Preserve the real per-person resolved legs/phase/cadences/queues/service/production plans/settlement/road-party and resident reservation/dwell/fairness/weather/support authority. Validate transform `spec_id`, `production_day`, `start_slot`, required-work progress and restock source IDs against those saved planners and the exact content manifest. Do not re-run Round seed or start a fresh job. |
| Character Round projections | `daily_round`, `you_sell`, `resident` are saved exactly in M2a2, including private descriptions and last published values. M2a3 must share pure projection/validation logic with each existing writer: match daily lines to saved resolved legs and strings; vendor list to saved vendor binding plus exact catalog; resident membership/patch/spot/phase/context to the saved resident controller and exact navigation content at its saved projection anchor. Preserve first-read values and perform the next update only on the ordinary Round pass; no reseeding or unconditional projection overwrite during hydration. If an existing cache can be stale, preserve its last-update anchor or validate its explicitly permitted stale relation instead of comparing it to a freshly advanced controller. |
| Geometry and static reference resolution | Complete manifest must bind exact nav/area/places/home/shelter content and actual generator/hash/build/target behavior. Validate saved ordinary place entries and patrol/generated routine/patch/spot/door references against their supplied compatible assets and saved Round memberships. Dynamic home IDs and ordered insertion authority remain saved, never regenerated. The component's finite position checks are not a geometry-manifest or route-reachability proof. |
| Clock/weather and Engine temporal context | Match the preserved WorldTime cache to the clock/Engine's saved processed position and cadence. Preserve weather, last-poll/movement/digest/round/clock/sound/bell/pollen anchors, owed work and host accepted time/debt/residuals. M2a1's numeric clock limits remain format policy, **not accepted effective-rate CPU limits**. |
| Remaining World authority | All knowledge/fact/pollen, notices/custody/marks, dogs, weather/shelter/context, switches, immutable mappings, area adjacency and semantic ownership remain mandatory later payloads. World.speech_actions derives exactly from saved SpeechRouter tasks, with complete root cross-validation. |
| Scheduler/Night/conversation/floor/speech | Preserve semantic pending work, drained input receipts, held results/retry lanes/fairness, private attention/history/conversation/counters, and interrupted unsent draft disposition. No recording resumes speaking because a component decoded. |
| Complete envelope/capture/hydrate/host | Compose every owner without optional omitted authority, build and resolve the exact manifest, then perform whole-world time/reference/root/count/byte validation before M2b candidate adoption. Engine hydration, retries, save slots, files/UI and host execution are not implemented here. The candidate type cannot replace a World through a production API. |

M2a3 should therefore compose **Round and resident/production/household/road-party authority** against this actor/item/place foundation. Additional owner cuts follow before the complete envelope. Do not begin that work until M2a2 is reviewed and committed.
