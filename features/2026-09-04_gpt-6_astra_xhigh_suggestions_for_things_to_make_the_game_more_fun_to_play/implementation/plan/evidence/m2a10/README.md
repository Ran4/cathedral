# M2a10 existing social checkpoint handoff — 2026-09-08

Status: Implemented, independently reviewed and accepted (2026-09-08). Existing component scope; complete M2/M3 remain pending and global caps are unchanged.

[Owner coverage](OWNER_COVERAGE.md) accounts for every Conversation, WarmExchanges and Novelty field and Engine's initial idle configuration/player binding. [Admission](ADMISSION.md) proves closed records and raw-byte lifetime charges with a 64 KiB fixed working allowance under unchanged 128 MiB E/J, depth 64 and 1 GiB shared ceilings. Historical references, raw config IEEE bits, opaque meeting/context salts, saturating tokens and logical coordinates remain exact. No decode-time expiry, seed, context hash or scheduler reconstruction occurs. Interrupted unsent microphone/STT captures remain excluded under the later speech draft policy.

Four new component fixtures preserve earlier fixture bytes. Fourteen owner tests include independently prepared and deliberately scrambled installations with immediate canonical equality, ordinary same-time group/old-token continuation, partial/backward dwell, independent reciprocal/invitation state, warm/Novelty expiry, late inbox news, raw config, malformed fields/counts/IDs, sparse and 25,000-row maxima, byte refusal and reservation release. Six independent public tests cover observable continuation and public admission. The final all-checkpoint suite passes 133 tests with 21 ignored; the same serial original supplies all three concrete social layout lines. The final public suite passes 6/6. The full offline workspace passes 2,045 tests across 42 result-bearing targets, with 29 ignored and no failures; [verification](verification.json) records exact totals.

[The source freeze](source_freeze.json) pins 895 source/input files using the shared v2 union scope, including default_config.ron. [Coordinator source audit](coordinator/source_audit.json) confirms all 52 historical fixture/doc files remain unchanged and ordinary code differs only by module wiring plus the two superseded attention comments. [Installed allocation proof](development/stdlib_allocation_audit.json) and its [independent audit](coordinator/stdlib_allocation_audit.json) bind seven standard-library files and exact excerpts to the recorded rustc sysroot/version. [All 18 command records](commands.json) retain exact original output, source-at-start manifests, environment, deterministic gzip archives and honest failure history. [Environment](environment.json) binds the compiler and build-variable provenance. [Owner finalization](development/owner_finalize.json) verifies every archive and the unchanged source map.

Actual authored 520 and successfully placed +2000/2520 debug smokes are [retained verbatim](development/authored_smoke.json) [for both workloads](development/populated_smoke.json), including all six raw timing arrays, input/output speech witnesses and counts. They establish functional probe behavior only. Coordinator release measurements and independent acceptance are recorded below. Full envelope/content/build/target/toolchain/DefaultHasher agreement, all-consumer horizons, true Running/Save/Load/retiring lifetimes, remaining Engine/scheduler/speech/host owners, M2b hydration and M2c pending-work adoption remain mandatory. Naive backbone+Round Save+Load already exceeds 1 GiB; no cap is raised.

[Coordinator acceptance](coordinator/review.md) verifies 2,045 workspace passes, exact source/log/admission evidence and [3,600 release phase samples](performance/README.md). Authored/populated export p99 is 0.004127/0.005404 ms. Seven historical datasets and four negative auditor checks pass their intended gates.

Three development failures remain in the command archive: missing test imports/type inference, a test constructor that disabled the scripted cognition capability, and a probe witness that tried to serialize Vec3 directly. The corrections supply the test types, enable the intended scripted capability, and encode coordinates through to_array. No assertions were weakened. The earlier uv/cache and harness setup failures are retained separately in [setup notes](development/setup_failures.md).

M2a10 [tail review](coordinator/tail_latency_audit.json) retains a slow third populated run: pooled populated export p99 is 3.577735 ms, maximum export 13.120826 ms and maximum decode 55.358842 ms. The smaller figures above are medians of per-run percentiles; they do not establish synchronous host-frame acceptance.
