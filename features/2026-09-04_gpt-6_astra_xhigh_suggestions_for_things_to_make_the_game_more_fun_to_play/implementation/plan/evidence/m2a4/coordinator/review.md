Status: Accepted M2a4 component cut (2026-09-08). M2 complete-envelope and adoption gates remain pending.

# M2a4 coordinator review

The strict WeatherTimeline, World climate/context and Engine clock/bell components preserve their covered private authority without constructor sanitation, reseeding or a production replacement API. Source review, independent public tests, continuation/corruption/admission witnesses, full workspace verification and repeated release measurements pass. The cut is ready to commit; it does not implement a complete saved city.

The four independent public tests verify disabled zero-frequency explicit overrides and retained inactive residue; changed sound percepts under equal IDs; sampled time against an unadopted backbone; and matching corrupt weather copies that disagree with the retained timeline. Private continuations deliberately scramble covered fields, restore them and compare immediate canonical bytes before any poll can overwrite a missing cursor. Later polls preserve lightning IDs, water/residue and old-slope bell deadlines exactly. Initial clock provenance is validated at its own origin; only the live segment must project to the saved boundary.

Final workspace: **1,928 passed, 0 failed, 13 intentional ignores across 36 targets**. Focused checkpoints: **50/0/5**; public Character/Round/climate cases: **13/0/0**, included in the workspace. The [log audit](log_audit.json) independently matches 14 raw logs, 13 timed-command records, both source manifests, 10 layout values, two development diagnostics and two release-build artifacts to their archives and reported results. Original output remains intact; archives use exact gzip or only redundant terminal-LF removal.

The [source audit](source_audit.json) verifies all 95 final hashes, all 16 changed implementation/fixture paths, all 11 unchanged historical fixtures and unchanged ordinary Engine/weather logic. All 10 Rust files pass the [scoped format audit](format_audit.json). The successful first workspace has an immutable [earlier manifest](pre_assertion_source_hashes.json); its only subsequent source change is the immediate-restoration test assertion. Final focused/workspace runs and release measurements use manifest SHA-256 `8aec88e3c25b186ce96a6a237f3ac61861f9bac5c807e8980ab9baef237ffd27`.

The [release review](../performance/README.md) audits 3,600 raw phase samples on 520/2,520-character worlds. Both retain a 2,316-byte payload and 403,672-byte standalone Save+Load charge excluding Running. Median per-run export p99 is 147.116/147.210 µs; the maximum individual phase sample is 152.234 µs. All six phases meet the 2 ms budget in this forced-storm/three-bell workload. Scheduled weather uses a different validation path and is not assigned that latency bound. The auditor also passes a historical 3,600-sample regression and refuses three independently corrupted measurements.

The reviewed 64 KiB working allowance exceeds the 22,784-byte conservative sampling-buffer bound and remains attached in addition to the original raw-input charge. Exact sample/time/sound-switch consistency is checked against existing backbone authority. Format limits for custom climate, queue count and time remain explicit restrictions. Existing long-span forced-lightning and office catch-up enumeration, complete effective-rate/accounting horizons, other private owners and full Engine/World/host composition remain mandatory before adoption. The prior M2a2+Round coexistence overage and previous synchronous-frame failures are unchanged; no budget is raised and no renderer/full-stress claim is added.

Next coherent owner cut: knowledge/facts/pollen and its geography/area-adjacency contract, followed by remaining conversation/attention, law and scheduling/speech owners. Source work for the next cut begins only after this commit.
