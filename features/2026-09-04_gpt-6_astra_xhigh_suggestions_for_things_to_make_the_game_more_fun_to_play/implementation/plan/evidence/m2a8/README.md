# M2a8 existing animals and shared movement cadence — 2026-09-08

Status: Implemented and coordinator-reviewed (2026-09-08). Final workspace and release audits pass. Existing component only; complete M2 persistence and M3 adoption remain unimplemented.

WorldAnimals/EngineAnimals preserve the whole ordered existing dog pack, every private path/rest/epoch value, independent exact World/Engine navigation bindings, immutable player ID, exact dogs_published and the shared human/dog movement_now anchor. Arrival's positive last speed, planted turns, partial acceleration, signed historical edits and wrapping u64::MAX decisions survive without reseeding or rerouting. Candidates are opaque and read-only.

- [Coordinator review](coordinator/review.md) accepts this component. [Release measurements](performance/README.md) retain 3,600 independently audited phase samples, with export p99 of 0.030/0.032 ms.
- [Owner coverage](OWNER_COVERAGE.md) lists all fourteen Dog fields and shared cadence/publication ownership, supported reachability exceptions and remaining whole-envelope dependencies.
- [Admission](ADMISSION.md) proves closed layouts and bounded borrowed-ID scratch, retaining original raw charges under unchanged caps. No registry/navigation clone or cache warming occurs.
- [Source freeze](source_freeze.json) and [841-file manifest](source_hashes.json) pin final source, tests, fixtures and runtime assets. [Owner scope audit](development/source_scope_audit.json) verifies unchanged ordinary code after only module wiring and all 22 historical JSON fixtures plus README unchanged.
- [Commands](commands.json) retain every command, explicit environment, source manifest from START and exact raw stdout+stderr hashes. [Compiler/environment](environment.json) records rustc 1.96.0 x86_64 Linux, fixed cargo settings and the build-variable whitelist.
- [Archive verifier](development/archive_verify.py) checks deterministic gzip archives, original/archive/decompressed hashes and the unchanged final source. [Verification](verification.json) records all 11 successful command archives and the unchanged final-source check.

The frozen-source full workspace passes **2,004 tests, 0 failures and 27 intentional ignores across 40 targets** with `cargo test --offline --workspace -j1` (915.63 seconds). Final focused verification passes 104 tests, no failures and 19 intentional ignores. The six independent public boundaries pass, including unadopted backbone/exact nav, edited/reordered pack, planted-turn continuation, positive-speed stop, raw admission and independent Engine no-nav behavior. Twelve private tests cover strict mandatory/duplicate/numeric refusal, exact golden bytes, signed rest/edit policy, maximum 65,536 path/25,000 identity shape, wrapped successful/failed drift, later deterministic decisions, and Engine publication/cadence continuation. The human/dog test confirms neither moves before the saved fractional slice becomes due. Initial dog publication uses a backend that panics if cognition is requested; ordinary source remains unchanged.

The final layout diagnostics pass: Dog 200 B, DogCoat 1 B, Vec3 24 B, DogId 24 B, borrowed ID 8 B, World DTO/candidate 72 B, Engine DTO/candidate 152 B, LogicalTime 8 B. A sorted temporary Vec of borrowed dog IDs has a conservative capacity bound below 400,000 B at 25,000 rows, under the 1 MiB working allowance. Pack order itself is untouched.

Only two new supported component fixtures are added: world_animals.json contains all six coats, ordered moving/turning/resting/stop states, failed retry, signed rest and wrapping decisions; engine_animals.json contains the real post-slice path/body state, true publication and nonzero fractional cadence. They cannot load an entire saved city. The fixture creators refuse to overwrite existing nonempty files and never regenerate historical fixtures.

The two functional debug smokes use actual authored 520-character and +2,000 production placement (2,520) Engines with real navigation. alibi_animals_cost selects the same ordinary boundary in both: ten dogs, two active paths with sixteen waypoints, eight resting, one moving, one turning and two spent decisions. The first resting publication occurs once, the quiet sub-slice dedupes, and twenty-seven later movement publications demonstrate acceleration and yaw-only turning. Boundary is 6.061000000000001 s, movement anchor 6.0499999999999865 s and residual 0.011000000000014332 s. Rare arrival/retry/wrap/max cases are tests outside this small timed workload.

No development verification/probe command failed. All commands retained a source manifest from START; the initial two commands retain explicit environment settings, while subsequent and all final commands additionally record the inherited build-variable whitelist. The final scoped formatter changed only eight new Rust files; coordinator read-only checks also cover the two ordinary module-wiring files.

Both modes encode 4,188 B with E 70,590 B, working allowance 1,048,576 B and per-cohort peak 1,347,596 B. Simultaneous Save+Load retains 2,695,192 B excluding Running. The standard six timing arrays are preserved under scenario animals-obligations-v1; coordinator release repetitions establish cost evidence separately. Debug smokes precede final whitespace/test-only additions and do not claim final release measurements.

Complete envelope/build/target/toolchain/DefaultHasher binding, all-consumer numeric/temporal safety, host accepted residual/player agreement, full Engine configuration equality, initial host republish, capture/hydration and pending external work remain required. Ward moods/Night and remaining Engine/attention/conversation/speech/scheduler owners are next separate cuts. Existing naive backbone+Round Save+Load already totals 1,256,093,444 B before Running, exceeding 1 GiB. This component does not solve full phase/lifetime admission or synchronous host placement, and no global cap increases.
