Status: Accepted as an existing component cut (2026-09-08); complete M2 remains pending.

# M2a8 coordinator review

WorldAnimals and EngineAnimals preserve the ordered private dog pack, including exact paths, rest counters, decision epochs, metadata and body/motion values. World navigation and the Engine's separately retained navigation bind exact immutable source fingerprints. The shared human/dog `movement_now` and `dogs_published` flag survive independently of the pack's current contents. No seed, route calculation, navigation cache warming, movement tick or partial production install occurs during decoding.

All six independent public tests pass. They continue a reversed and renamed resting pack through its next drift, preserve an ordinary planted turn and subsequent decisions, retain the positive-speed/empty-path stop edge, refuse changed/missing navigation while binding the original to an unadopted backbone, retain a pack with no navigation, and check malformed records and raw padding admission. The Engine test distinguishes its absent navigation from a later public World navigation edit while preserving its unspent publication and exact old movement anchor.

Private tests restore into independently prepared owners after scrambling covered state and assert immediate canonical equality. Ordinary steps then cover partial acceleration, planted turning, arrival, a quiet rest, failed routing and the wrapping `u64::MAX` decision counter. Engine continuation compares dog publications, authority, movement-anchor bits and world revision across ordinary polls. Initial resting and empty-pack publication flags, stop-once behavior, inactive/backward elapsed anchors and distinct World/config navigation remain explicit cases. Dog IDs retain the common validity rule, including spaces; dog identity is independent of character identity.

The frozen-source workspace passes **2,004 tests, zero failures and 27 intentional ignores across 40 targets**. Final focused checkpoints pass 104 tests with 19 intentional ignores, and both layout diagnostics pass. The independent [log/archive audit](log_archive_audit.json) verifies all 11 command records, including nine Cargo invocations, the scoped formatter and compiler output. Every command retains a source manifest from its start; final focused/layout/workspace records share the frozen source. No development verification command failed.

The independent [source audit](source_audit.json) verifies all **841 frozen source files**, all 12 changed source/fixture paths, and 22 unchanged historical JSON fixtures plus their README. All ten changed Rust files pass scoped formatting. Removing only the new checkpoint module declarations makes the ordinary dog and Engine source byte-identical to HEAD. Frozen manifest SHA-256: `4ed36cbf1e4292bd03a512eb8aa2c55dfb7422f08681f4697a2c32e707754c29`.

The [admission proof](../ADMISSION.md) binds the concrete Dog (200 B), World DTO/candidate (72 B) and Engine DTO/candidate (152 B) layouts. A minimum dog row contributes 4,032 expanded bytes; each 24-byte waypoint contributes 704. The temporary sorted borrowed-ID vector remains below 400,000 bytes at the 25,000-dog shape ceiling, covered by the attached 1 MiB working allowance. Sorting changes only the index, preserving the saved pack order. The maximum path has 65,536 waypoints, while aggregate byte caps still limit all combined records. Raw padding and escape charges remain attached through candidate validation; failed validation releases the lease.

The [release measurements](../performance/README.md) independently audit six runs and **3,600 raw phase samples** on one unchanged executable. Authored/populated export p99 is **0.029642/0.032386 ms**, the median of three per-run percentiles; maximum individual export is 0.042930 ms. All runs retain the ordinary concurrent resting/moving/turning pack, exact fractional cadence and once-only resting publication. Save+Load reservation is 2,695,192 bytes excluding Running. All five historical component datasets pass the amended auditor, and three deliberately corrupted datasets fail the intended percentile, allowance and publication gates.

This is an existing component acceptance. Finite signed motion values and preserved logical anchors do not by themselves prove every consumer's future arithmetic and timing horizon. Complete behavior/build/target/toolchain resolution, the full World/Engine/host envelope, shared human/dog cadence agreement, host initial republish, pending external work and Running/Save/Load/retiring lifetimes remain required. The naive backbone+Round Save+Load reservation already exceeds the unchanged 1 GiB shared cap; full integration must establish actual phase lifetimes. Other components' capture costs still require bounded offload or incremental host coordination.

Independent ward moods/Night and the remaining Engine configuration/cadence, conversation/attention/scheduler/speech and pending-work owners remain separate work. No complete M2, renderer or full-stress acceptance is added.
