# M2a3 owner coverage — 2026-09-08

Status: Implemented and reviewed; focused/public, full workspace and coordinator release audits pass. Complete envelope and host acceptance remain pending. Base M2a2: `3e9c93f5354176b2f506c50ca4de5c63e1de7f17`.

This is the complete private **Round component**, composed against borrowed real M2a2 authority. It is not a World/Engine envelope. Every runtime field appears in the explicit records, except the deliberately empty ladder scratch described below. Nullable fields require an explicit null; collections preserve order and reject duplicate map/set identities.

| Owner | Exact saved authority and validation policy |
|---|---|
| Water sources | Name, point, declared static sound, keeper, ordered queue, serving actor/deadline and keeper sound deadline. Exact installed source order/geometry/sound; current keeper and queued actors resolve. Household insertion may precede the currently served drawer: membership, not queue-head equality, is required. Queue/controller bindings agree. |
| Food stalls/trades | Full resolved trade occupations/listings/restock/per-serving declaration; stall name/site/pitch/trade/vendor/queue/service/preferred handle/open predicate/cry deadline. Installed content, catalog, actual stock quantities and shared pitch resolver must agree. Preferred authored handles may be absent in a partial cast; actual vendors must exist. Manual restock provenance remains Inventory-owned; `legacy_stall:` sources bind declared stall/trade/item identity independently of today's vendor. |
| Townsperson | Home/base, ordered resolved legs and labels, leash/curfew/source/household flags, independent food errand and eating item/timer, phase/travel target/intent ownership/motion cause, next decision, epoch, displaced evening seed, lag share and excuse flag. Home matches the saved household register. Eating item IDs can become historical between ordinary passes. Pending deadlines are not forcibly expired at decode. |
| Services and temporal cursors | Last office days, last need/game days, seeded flag, weather shelter intents and hysteresis, logical lightning/chalk deadlines, calendar knowledge refusal deadlines and experienced-buyer set. Preserve overdue/stale state until its normal owner pass. Both pollen deadline map and actual due index are saved, including legitimate stale re-enrollment entries. Every live deadline has a matching due key; keys have canonical signed numeric order. Only the pollen initially-due NEG_INFINITY sentinel becomes Never. |
| Residents | All people records (patch/preferred/current/target, phase, dwell/epoch/retiring, prior support eligibility, missed meal, remaining recovery, weather intent/slot/arrived/retry and projection sample); reservation indexes; order/cursor; optional/changed/departing sets; local shelter indexes. Generated routine identity/preference and current destination/weather claims agree. `spot=None` may retain an occupied origin until 0.85m clearance. One actor may use the same occupied and destination ID; two actors cannot. |
| Resident projection sample | Exact sampled time, phase/spot/target/weather target, occupied handle, dwell anchor, pose/walking, housed/optional, movement cause and shelter result. Shared pure projection code validates saved Character.resident using that sample, not the later live controller. Speech interruption, later motion and Engine actions can leave the cache legitimately old. Ordinary publication keeps its old sparse-change predicate and reuses occupied text storage. |
| Other Character projections | Daily prose uses the shared leg formatter for ordinary/keeper/reroll/edit/road writers and the shared resident daily prose formatter. Vendor prices use the same catalog/template helper as ordinary binding. Seeded Round rejects orphan vendor/resident cache values. Decode neither refreshes nor silently clears any projection. |
| Lamps | Ordered square/position/lit/keeper records, revision, night day, keeper target map and Belwyn square/index map. Static geometry and declared beats agree; target binding is exact. Lit/current-pose/deadline values are not recomputed on load. |
| Market | Worksites, counters/groups, ordered stock plans, actor-keyed errands (selected and seen exact session bindings, phase/spend/failure fingerprint/deadline/hold anchor), closed visits and reasons. Retained declarations match installed definitions; multiple plans may share a buyer. Seller absence can suspend a binding and preserve its deadline/spent budget; a waiting hold marker need not already have a walk deadline. |
| Production | Ordered producer plans and transforms, producer/day start counts, preceding calendar sample and eligibility map, stranded-job watches. Retained immutable recipe/work/site/offset definitions match installed content through shared pure resolvers. Old jobs need not retain pruned day counters. Missing plans/specs leave Inventory obligations for the ordinary watchdog; no plan is recreated. |
| Manual transform boundary | `World::start_transform_job` has no origin tag. Valid manual jobs can use arbitrary job/slot/input/output values and can name a known spec. The spec supplies work eligibility/cadence; Inventory owns that job's exact recipe/identity. Checkpoint validation cannot infer a generated origin from an ID prefix. Actual planner continuation tests pin its generated job ID, recipe and exactly-once completion behavior. |
| Household/road accounting | Working reserves, independent Watch sampled/successful settlement days, zero streaks, cumulative payroll/redistribution/road cash totals. Do not equate failure and success stamps. V1 refuses totals without room for a maximum-population next accounting pass; the complete clock/rate horizon remains a later validation gate. |
| Road parties | Identity/leader/ordered remaining members/gate geometry/schedule, original wallet floats/cargo/manifest/legs, phase/trip/last trigger/departure excuses. Remaining members form an ordered declared subset; permanently stranded members may remain in original float/history maps. Exact presence epochs and inventories belong to M2a2 and continue through normal transitions. |
| Retained diagnostics/publication | Food log (4,096 rows), observed cart-load cache, departed-this-tick notifications and resident changed set are preserved exactly, including unread publications. Whole Engine flush/root validation is pending. `ladder_scratch` is required empty at export and omitted from the wire; its capacity is transient. |
| Immutable context/indexes | Exact raw navigation JSON+bitset fingerprint excludes lazy path-cache warmth. Catalog, shelter definitions and embedded Round/Food/Homes inputs have exact SHA-256 bindings. Spot positions/owners and local shelter rows are saved and validated by pure streaming checks, without reseeding or allocating an expected city from external geometry. |

## Composition still required

M2a1 and M2a2 retain their accepted coverage. Remaining mandatory owners are World knowledge/facts/pollen, conversation/attention, law/custody/notices/marks, animals, weather/private clock state, area/static world context, switches/mechanisms, Engine sampled clocks/cadences/host accepted-time obligations, scheduler/Night/floor/speech and pending backend work. Their candidate-to-candidate temporal/reference/root checks must finish before a complete envelope or M2b adoption. The Round candidate has no production World/Engine replacement method; tests retain admission while cloning covered Round into an independently prepared control context.

The M2a1 clock ranges are numeric format policy, not CPU-safe effective rates. The focused signed-pollen fix prevents the nonpositive-calendar reinsertion loop, but other day/office crossing consumers and accounting horizons still need a complete-envelope gate. General saved place/patrol/area/household compatibility outside the Round bindings above is also pending.

Next proposed coherent cut: private clock/weather and sampled World climate/context authority, using these exact Round geometry references, followed by knowledge/conversation/pollen and law owners before scheduling/speech composition. Final effective-rate acceptance depends on every crossing consumer; a climate component alone cannot certify it. No M2a4 starts before review and commit.
