# Provisional probe review — 2026-09-08

The first authored_smoke and populated_smoke commands exited successfully and retain all original JSON samples/witnesses. They are **not accepted timing setup**: the quiet interval jumped from 0.05 to 5.0, causing the ordinary Engine diagnostic `[time] coarse poll discarded 4.550000s of physical work`. Whole failure/held Debug messages also included excessive unrelated publication text. Coordinator source/witness review caught this before workspace/release acceptance. The original `source_freeze` record and checkpoint_final command are retained with their exact maps/logs; provisional freeze copies are here. The final probe must use bounded ordinary polls, assert zero coarse-discard messages, report poll count/max step, and retain compact relevant messages plus a diagnostic count/digest of the complete stream. Owner production code is unaffected.
