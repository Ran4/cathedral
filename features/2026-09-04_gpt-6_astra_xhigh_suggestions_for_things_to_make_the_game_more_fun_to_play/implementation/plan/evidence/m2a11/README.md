# M2a11 scheduler component handoff — 2026-09-08

Status: M2a11 component implemented, verified and independently accepted (2026-09-08). Complete M2a/M2b/M2c and host adoption remain pending.

`NpcSchedulerDtoV1` and `EngineSchedulerDtoV1` preserve the existing scheduler authority as strict admitted read-only components. [Coverage](OWNER_COVERAGE.md) maps all fields and continuation obligations; [admission](ADMISSION.md) proves raw lexical charges, closed owner layouts and sequential working scratch under unchanged caps. No constructor, seeding, prompt rerender, ordinary poll, external retry, public Clone or production partial installation is introduced.

Public APIs live in `scheduler::checkpoint` and `engine::scheduler_checkpoint`: borrowed live/saved contexts, export/preflight/decode, attached encode/candidate conversion, and read-only scheduler/configuration/count inspection. Standalone semantic request IDs retain all u64 values. Candidates can be re-exported by borrowing their scheduler, while only owner-private tests install fields to prove continuation. Engine original float bits are independent of actual normalized scheduler pacing.

The production `alibi_scheduler_cost` example loads actual authored content/navigation and +2,000 placed actors, then uses ordinary speech, scripted provider terminals and microphone floor hold to create pending protected work, a queued follow-up, handoff/retry courtesy and an exact held result. It records all six phase samples, exact submitted prompts/output budgets and semantic counters. It changes no private Engine, World or scheduler fields to manufacture the measured boundary. Rare/max states are deterministic private tests, not timing workload claims.

`development/run_checked.py` records every substantive verification command's original combined stdout/stderr and SHA-256, source-at-start map from committed `component_input_sources.py` (`component-inputs-v2`), helper hash and actual environment before execution. Final freeze includes the coordinator's public test and all new fixtures. The record retains failures and exact gzip archives with mtime zero. One initial uv flag-position setup error precedes the runner and is described in `development/setup_failures.md`; no mutation occurred in that failed call. GPU/window availability is not reprobed.

Final focused/public/workspace totals, debug metadata, source freeze and exact archives are recorded in [verification.json](verification.json), [commands.json](commands.json) and `development/`. All 20 recorded command originals and their source-at-start manifests were verified; the initial development failures and provisional measurement setup remain preserved. Focused verification has 23 ignored generators; the public suite has none. Cargo was explicitly ceded to the coordinator after the serial workspace exited successfully. Coordinator owns release builds, all timings including long tails, independent review and final commit. No push is authorized from this owner.


Four new component fixtures are under `crates/cathedral-sim/tests/fixtures/checkpoint_scheduler`: empty/never-paced, exact unfinished idle input, closed held error with queued follow-up, and Engine original-config bits. Earlier fixture bytes are unchanged. Eighteen new private tests cover exact restoration before continuation, fairness/FIFO/suppression, flying same-actor follow-up, Busy/failure courtesy, closed held success/error/oversized replies, stale epochs, committed-root replay, receipt-capacity deferral/release, borrowed saved context, strict malformed/raw allocation and maximum supported shapes. Two fixture generators remain explicitly ignored in ordinary runs.

The corrected final debug files are `development/authored_bounded_smoke.json` and `development/populated_bounded_smoke.json`: 136 polls, max 40 ms (floating epsilon), zero discarded physical work, all 2,000 additions placed. The original successful-exit smokes and provisional freeze remain untouched with their measurement limitation documented in `development/provisional_probe_review.md`. Their coarse quiet-interval jump makes them unsuitable as final timing setup. Final frozen input scope is 907 paths, SHA-256 `180cb18a172692c1be64bdcedf646ed446905948fbf48055ce139bd515c62be9`.

Coordinator acceptance verifies 2,069 workspace passes, 151 focused passes, six public boundaries and all 3,600 release phase samples. The [review](coordinator/review.md) and [performance record](performance/README.md) include all long samples and explicit full-save/host limits. authored export pooled p99 0.077532 ms, observed maximum 0.093167 ms; populated export pooled p99 0.186618 ms, observed maximum 0.209647 ms.
