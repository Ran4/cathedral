# M2a11 scheduler admission — 2026-09-08

Status: Source/layout proof, 151 focused checks, 6 public boundaries and the serial full workspace (2,069 passed, 0 failed, 31 ignored) passed on the final 907-path freeze. Independent coordinator release review passed; see [review](coordinator/review.md). No budget increases: 128 MiB encoded J, 128 MiB lexical expansion E, nesting depth 64, shared Running/Save/Load/retiring ceiling 1 GiB.

The nonallocating lexical meter streams a borrowed owner before exact copying and the complete raw input before typed decoding. Each retained cohort reserves at least `4,096 + 4*E + 3*J + 4,194,304` bytes. E charges 512 per object/array, 64 per scalar/string/key and twice original encoded string bytes. J includes padding and escapes. The three-J allowance covers original input and geometric JSON escape scratch. Candidate conversion moves the already admitted DTO and never shrinks raw charges; disposal releases the lease after owned allocations. Production export/preflight never materializes a JSON Value.

The 4 MiB working allowance covers sequential borrowed ledger validation, lane sorting and root sorting, plus fixed serializer/recursion/error-framing state. Saved ledger validation admits at most 256 roots and 4,352 receipt IDs/ordinals. On the pinned 64-bit standard library a deliberately loose bound allocates one full node per entry: node bytes `16 + 12*8 + 11*(key_size + value_size)`. For 16-byte OperationId, 24-byte CommandId and 8-byte ordinal this is `256*288 + 4352*(376+200) = 2,580,480` bytes. Add 65,536 bytes for fixed serializer/recursion/error framing and remain below 4 MiB. Input-derived error text is a variable allocation: a malformed unknown field name may exceed 64 KiB, and its owned error/format copies are charged to the conservative E/J allowances, not this fixed framing allowance. The 65,536-byte figure does not cap arbitrary parser diagnostics. Actual nonroot nodes hold at least five entries, making this bound conservative. The ledger sets are dropped before scheduler scratch begins. No temporary ledger candidate, World, geometry or registry index exists.

Both queue counts are checked before building their borrowed index. Exact-size collection of at most 50,000 actor references needs 400,000 bytes, with unstable in-place sorting. That scope ends before at most 257 OperationId values are allocated with explicit capacity: 4,112 bytes. Even a conservative doubling of either vector fits with fixed work below 4 MiB. Sorting changes only references/roots, preserving weighted order and FIFO data. Root lookup borrows ledger records, including known committed receipts, and never clones them.

Raw malformed overcounts are bounded before their typed collections exist by the same lexical charge. Direct retry-map visitors reject duplicate keys before decoding an overwritten value; no temporary map/Value is built. Each minimal retry row contributes an actor key (64 E), a 512 E object, two field keys, incarnation scalar and a nested semantic object with its scalar fields. Even the deliberately loose one-node-per-entry bound for the concrete 24-byte ActorId and 24-byte RetryWork is 640 bytes, below that row charge. Empty/singleton BTreeMap clones are covered by the outer 512 E object; pinned stdlib clone drops a formerly empty retained root and otherwise preserves a bounded tree shape.

Vec/VecDeque fields hold 24-byte ActorId or String values. Their doubling capacity costs below 48 bytes per populated row, versus at least 64 E per string plus content, and the four-slot singleton floor is 96 bytes covered by the 512 E array. VecDeque clone allocates from populated length rather than historical source capacity; String and Vec clones copy populated content. Raw strings charge their full encoded bytes, covering UTF-8 copies and escaped parser allocations. The owner, flight and completion inline layouts are covered by their outer objects and mandatory field keys; final focused layout output checks actual sizes. Input buffers and prompt are direct bounded text adapters; held success reuses the 400,000-byte Completion wire adapter without applying Night's narrower scalar predicate.

Maximum deterministic shapes exercise 100,000 order slots, two disjoint 25,000-row lanes, 256 retry roots, 64 rows of 65,536 bytes in each input buffer, 65,536-byte prompt and 400,000-byte exact held text. Historical IDs need no current body; raw malformed one-over counts and duplicate roots remain refused. These maxima are independently subject to combined E/J limits, rather than promising every conceivable maximum-length ID/text combination fits. The timed probe uses ordinary authored 520 and successfully placed +2,000/2,520 Engines and keeps real provider inputs; maximum-layout tests are separate from measured timing.

Failure-count admission protects one ordinary increment through i32::MAX and the following `as i32 - 1`; it does not certify an infinite outage. Reachable +Infinity pacing is explicit Never and finite historical future anchors survive exactly. Full all-consumer horizon/rate proofs, protected-root owner agreement, actual cohort lifetime coordination and host offload/incremental capture remain pending. Naive backbone+Round Save+Load already totals 1,256,093,444 bytes before Running, above 1 GiB. This small owner cannot establish complete-save or synchronous host-frame acceptance.


Frozen x86_64/rustc 1.96.0 layout diagnostics: NpcScheduler 376 B, InFlight 136 B, RetryWork 24 B, Completion 64 B, ActorId 24 B, OperationId 16 B, CommandId 24 B, retry-node upper 640 B, scheduler DTO/candidate 392 B each, borrowed scheduler context 96 B, Engine DTO/candidate 432 B each and Engine context 104 B. Final focused `checkpoint_bounded_final` emits both scheduler_layout lines serially; no separate repeated layout run is needed. The [installed standard-library allocation audit](development/stdlib_allocation_audit.json) pins exact source bytes/excerpts for BTree node occupancy/layout and cloning, VecDeque populated-length clone, String/Vec copies and RawVec growth to the recorded rustc version/sysroot.

Both final debug smokes use 136 ordinary polls, maximum elapsed step 0.040000000000000036 s and **zero** coarse physical-work-discard diagnostics. Authored 520 has order 719, one held protected flight, one queued same-actor follow-up, one handoff, one retry and failure count 1. Its three actual prompt sizes are 19,849/19,656/20,119 B; current held prompt 20,119 B, both saved input buffers 1 row/141 B, held reply 59 B. J = 27,444 B, E = 111,932 B, per-cohort peak 4,728,460 B, Save+Load 9,456,920 B excluding Running. Populated 2,520 has all 2,000 additions placed and order 2,719, same semantic counts, input buffers 1 row/136 B and held prompt 19,295 B. J = 44,596 B, E = 266,236 B, peak 5,397,132 B, Save+Load 10,794,264 B excluding Running. These are functional debug witnesses, not release latency results or maximum-shape/host limits.

The first provisional smokes used a coarse quiet-interval jump and emitted a discarded-physical-work diagnostic. They remain unchanged as `development/{authored,populated}_smoke.json`, together with the provisional freeze and command originals, but are excluded from final accepted setup. Corrected `{authored,populated}_bounded_smoke.json` preserves exact original prompts/budgets and compact relevant scheduler messages, while a labeled nonsecurity FNV1a64 digest/count covers all debug-formatted Engine messages without retaining enormous motion-publication text. The probe routes **every** poll through the bounded driver and refuses any coarse-discard diagnostic. Coordinator review requested this measurement correction; owner production behavior was unchanged.
