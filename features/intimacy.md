# Intimacy

Parked from `features/implemented/extra_pockets.md` ("the intimacy seam") — not
started, not scoped.

Making love is its own feature with its own problems: relationships, consent,
privacy, what the camera does. What the pockets feature already provides is the
**container side**: fluids can be ordinary catalog kinds that sit in any body
slot like anything else, with provenance as metadata.

Tension to resolve here: provenance-as-id conflicts with the unknown-people
rule — notices solved this by keeping ids private on the record and prose
public; the same split applies.
